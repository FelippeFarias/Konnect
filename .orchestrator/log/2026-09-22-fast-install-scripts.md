- 2026-09-22 INTAKE goal: add scripts/install.ps1 and scripts/install.sh that install a local Konnect build the way this PC runs it — build if stale, back up the current binary, copy it to the path the Claude Code MCP config actually uses, run konnect init (claude and optionally codex), point RETRACE_PYTHON at the repo's .venv-retrace, and verify — requested by the user ("Faça isso") after confirming the repo has no install scripts
- 2026-09-22 INTAKE route: FAST — two new files, no spec delta, no new dependency, reversible with one revert, no security or data surface in the repo (the scripts only act when a person runs them)
- 2026-09-22 DECISION: keep FAST's no-change-dir/no-archive/no-LEARN ceremony, but file the handoffs under .orchestrator/handoffs/install-scripts/ and add a reviewer pass after qa — WHY: orc merge's verify step (verifyDoneOnDisk) reads handoffs/<change>/ and requires both a qa and a reviewer DONE; the fast path's handoffs/fast/<slug>/ would leave the merge blocked — ROLLBACK: move the handoffs and drop the reviewer pass
- 2026-09-22 DECISION: documenting the scripts in DEV.md/README.md is queued, not done here — WHY: a third file breaks the fast lane's two-file bound; the scripts carry their own -Help/--help usage — ROLLBACK: add the doc line in this change and promote it to PROPOSE
- 2026-09-22 FAST developer 01 verified: commit 897c4a0 (scripts/install.ps1 526 lines, scripts/install.sh 581 lines, only those two files); orchestrator re-ran: PS 5.1 parser 0 errors, bash -n ok; ps1 and sh dry runs against the real ~/.claude.json name C:\Users\felip\.konnect\bin\konnect.exe, the .bak rename, init + status, and (with the main checkout's .venv-retrace) retrace 0.3.0 + the RETRACE_PYTHON set; real konnect.exe hash unchanged, RETRACE_PYTHON still unset; -SkipBuild with no build refuses clearly; note: PROTOC is not in the user environment (sh warns in dry run) — asked the reviewer to judge probing common protoc locations
- 2026-09-22 FAST reviewer 03: FIX/implementation — SERIOUS: install.sh with --target pointing at a directory overwrites the old binary with no backup (install.sh:411-441; ps1 fails with a raw error); 9 MINOR (sh rollback unsafe when pasted twice; a failure after the rename prints no rollback; a failed restore reported as success; single quotes in paths break printed commands; staleness ignores docs/RELIABILITY_CONTRACT.md, root Cargo.toml, rust-toolchain.toml and CARGO_TARGET_DIR; a differing project-level entry is ignored silently; ps1 discovery: case-insensitive server name, POSIX path, relative -ClaudeConfig; jq<=1.6 exit 2 ambiguity; messages); PROTOC: keep the clear failure, add the persistent-variable hint, no probing of personal paths
- 2026-09-22 FAST qa 02: FIX/implementation — install.ps1 lacks [CmdletBinding()], so unknown or misspelled options (-WhatIf, -DryRum, -Bogus) are ignored and a REAL install runs with exit 0; the other 11 requirements PASS in both scripts
- 2026-09-22 INCIDENT: qa's exit-code probe `install.ps1 -Bogus` ran without its safety flags and, through that defect, replaced the real C:\Users\felip\.konnect\bin\konnect.exe with a fake test binary from 10:04:48 to ~10:06, running the fake's init/status (print-only); qa restored it with the script's own printed rollback; orchestrator verified at 10:09: sha256 DEA8824B…DEC5, mtime 2026-09-21T18:30:27Z, size 34333696, --version 0.12.0, bin folder as before, RETRACE_PYTHON unset (user+machine), ~/.claude settings/skills/agents untouched; the running MCP server (PID 30656) was unaffected
- 2026-09-22 DECISION: one developer repair round (the fast lane's single allowed round) covering qa's R2 fix, reviewer's SERIOUS and MINORs 2-10 plus the PROTOC hint; deferred: *.sh eol=lf in .gitattributes, schematic-viewer.exe staleness, -Client codex semantics, python -B — WHY: the ps1 defect is the most dangerous finding (a typo installs for real) and the rest are cheap in the same files — ROLLBACK: revert the repair commit; a second FIX promotes the lane to PROPOSE
- 2026-09-22 RULE for every later test of these scripts: each invocation carries -DryRun, or all of -Target <temp> -SkipInit -NoRetrace; real exe sha256 + RETRACE_PYTHON recorded before and after; the orchestrator re-verifies the machine after each delegation
- 2026-09-22 FAST repair verified: developer 04 DONE — commit 334a8ac (both scripts only); orchestrator re-ran: PS 5.1 parser 0 errors, bash -n ok; -WhatIf/-DryRum/-Bogus (with safety flags) exit 1 and write nothing; a folder target is refused by both scripts with the full-path hint and the existing file untouched; real exe sha256 DEA8824B… unchanged, RETRACE_PYTHON unset; settings.json changed at 10:16:45 by Claude Code's own model-settings reshuffle (konnect hooks 4/4 intact, Konnect skills/agents untouched) — not the installer; re-verify with qa + reviewer
- 2026-09-22 FAST qa 05: DONE — R2 and reviewer items 1-9 closed, no regression; "init fails after the swap" shown with a fake binary (rollback printed, exit 1, pasted rollback restores); real exe/RETRACE_PYTHON unchanged (orchestrator re-verified)
- 2026-09-22 FAST reviewer 06: DONE (ship) — all round-1 items + R2 closed, no new SERIOUS; MINOR 1: install.ps1 prints its rollback pair unguarded even in -DryRun, labelled "safe to paste twice" — pasting it after a dry run renames the intact binary away (recoverable by a rename; pre-existing since 897c4a0); MINOR 2: Format-PsLiteral (install.ps1:92) doubles only the ASCII quote, so a path with U+2018/U+2019 breaks the printed lines
- 2026-09-22 DECISION O: fix both MINORs before the merge as a small post-verify commit (guard the ps1 rollback pair in one Test-Path condition, label the dry-run block as a preview with no pasteable commands or clearly marked "only after a real run", double every PowerShell single-quote character U+0027/U+2018/U+2019/U+201A/U+201B), verified by the orchestrator with the HARD TEST RULE — WHY: the user is not an engineer and will run install.ps1; a dry-run summary that invites a harmful paste is the likeliest real mistake; both verifiers already returned DONE, so no FIX bounce — ROLLBACK: revert the commit
- 2026-09-22 DECISION O verified: developer 07 commit 6a2f7d2 (install.ps1 only; parser 0 errors; ASCII); orchestrator test in a temp target: -DryRun prints 0 Move-Item and no 'safe to paste twice' and changes nothing; a real run swaps the binary; the guarded rollback statement restores the old binary exactly on the first paste and changes nothing on the second ('rollback stopped'); real exe sha256 DEA8824B… and RETRACE_PYTHON unchanged -> GATE: merge needs the user's GRANT
- 2026-09-22 MERGE start: user recorded GRANT for install-scripts (sha 9a0f1ce = trunk head); orc merge install-scripts with the full battery (the user had tried running the script before the merge landed)
- 2026-09-22 MERGE done: orc merge install-scripts → main [6a2f7d2] (fast-forward 9a0f1ce..6a2f7d2), battery PASS (fmt --check, clippy --all-targets, test -p konnect, test -p konnect-core); an earlier attempt was cut off mid-battery when the session ended and left main untouched; push stays Tier 2
- 2026-09-22 PUSH done: user asked ("Envie o push"); git push origin main 9d33cf0..6a2f7d2 (93 commits, fast-forward) to FelippeFarias/Konnect; nothing sent upstream
