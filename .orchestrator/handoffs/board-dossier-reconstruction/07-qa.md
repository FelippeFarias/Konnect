---
change: board-dossier-reconstruction
task: VERIFY round 1
agent: qa
verdict: DONE
failing_layer: n/a
---

## Result

All 31 task criteria and all 23 spec-delta scenarios PASS at `8be8d66`, in the
worktree `C:\Users\felip\.orc\worktrees\konnect-3b7e2022\board-dossier-reconstruction`.
Four gates green: `cargo fmt --check` 0, `cargo clippy --all-targets` 0 (no
warnings), `cargo test -p konnect-core` 0, `cargo test -p konnect` 0 — re-run
with my two new test files in the tree. Both live retrace tests green under
`RETRACE_PYTHON`.

I added two test files and committed them as `bab650d`
(`test(photo-intake): …`, not pushed): nothing else in the worktree changed
(`git status --porcelain` clean, `git diff --stat` empty). They close the one
gap my checks found — `prepare_board_photo` had no call through the published
contract, and no test showed the design-brief checkpoint *re-opening*.

### Task criteria (31)

| # | Verdict | Evidence |
|---|---|---|
| 1.1 | PASS | `grep -n jpeg Cargo.toml` → `90:image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }`; `crates/konnect-core/Cargo.toml:37 image.workspace = true` — one occurrence, under `[dependencies]` (`:9`), `[dev-dependencies]` starts `:43`; crate builds (full test run exit 0) |
| 1.2 | PASS | `the_view_schema_requires_the_three_paths_and_computes_the_rest` ok; `photo_intake.rs:1894 "required": ["image_path", "project_dir", "map_id"]`; handler wired at `:1897` inside `photo_intake::tools()` |
| 1.3 | PASS | `a_cropped_and_scaled_view_is_saved_at_exactly_the_size_it_reports` ok, `exif_orientation_is_applied_before_the_crop_is_measured` ok; my `a_jpeg_crop_rotate_and_scale_survives_the_published_schema` ok — `source_size_px [60,120]` (oriented, not stored `120x60`), file on disk decodes to `32x32` = reported `output_size_px` |
| 1.4 | PASS | `a_map_id_no_scan_ever_assigned_is_rejected_and_mints_nothing` ok, `a_label_that_is_not_a_token_is_rejected_before_any_path_join` ok; mine rejects `../escape`, `sub/dir`, `back\slash`, `""`, 65 chars and asserts the whole project tree is byte-identical after |
| 1.5 | PASS | `mm_per_px_is_reported_only_when_the_saved_map_resolved_one`, `an_out_of_bounds_crop_names_the_oriented_dimensions_and_writes_nothing`, `a_photo_over_the_megapixel_cap_is_rejected_before_it_is_decoded`, `a_scale_outside_the_range_is_rejected_rather_than_clamped`, `a_view_longer_than_the_side_cap_is_rejected_before_it_is_allocated` — 5/5 ok |
| 1.6 | PASS | `registry_tool_counts_match_reality` ok; `router/registry.rs:123 tool_count: 6`; `photo_intake_exposes_its_tools_by_name` ok; my `the_loaded_toolset_publishes_six_tools_including_the_new_one` asserts the 6 names off the **router**, not the constant |
| 2.1 | PASS | `the_map_schema_declares_both_sections_optional_and_open` ok; `save_rejects_a_new_section_that_is_present_but_not_an_object` ok over `dossier`=null/string and `design_brief`=null/array; `a_map_with_both_new_sections_round_trips_through_save_and_load` ok |
| 2.2 | PASS | `fixed_records_are_closed_and_only_reviewed_maps_are_extensible` ok; `router/mod.rs:222-231` lists exactly the six D7 paths (`dossier`, `…/identity`, `…/physical`, `…/design_brief_seed`, `design_brief`, `…/physical_constraints`) with the shared "human edits this by hand" rationale — no seventh |
| 2.3 | PASS | `the_content_hash_of_the_fixture_map_is_pinned` ok, digest still `18afc7b999d88ad5…0908a`; `git show` of `fn hashed_fields`/`fn hashed_elements` at `1815948` vs `8be8d66` → `IDENTICAL`; the pinned test's own body → `IDENTICAL`; `every_schema_key_is_either_hashed_or_deliberately_not` ok |
| 2.4 | PASS | all five named tests ok: `adding_a_dossier_to_an_approved_map_revokes_its_approval`, `adding_a_design_brief_revokes_a_re_approved_dossier_only_map`, `resolving_a_scale_reference_changes_the_hash_and_leaving_it_unresolved_does_not`, `removing_an_approved_dossier_drops_the_section_and_revokes_approval`, `a_map_with_neither_new_section_survives_a_save_round_trip_unchanged` |
| 3.1 | PASS | `kicad-board-dossier/SKILL.md` exists; its one `load_toolset` example is `load_toolset('photo_intake')` (`:51`), a registered toolset; `documented_toolsets_exist_in_the_registry` ok |
| 3.2 | PASS | `references/dossier-schema.md` exists; `grep -c dossier-schema.md SKILL.md` = 1 |
| 3.3 | PASS | `kicad-design-reconstruction/SKILL.md` exists; `call_examples_name_real_parameters` ok |
| 3.4 | PASS | `references/design-brief-schema.md` exists; `grep -c design-brief-schema.md SKILL.md` = 2; `:148` reproduces the D5 `layout-methodology.md` section-1 mapping row for row, `:75-77` the `resolution_status`/`search_terms_used`/`candidates` rule, `:166` the `unresolved[]` convention |
| 3.5 | PASS | `kicad-photo-to-board/SKILL.md` exists; stage table `:54-63` names `pcb-photo-intake-agent`, `pcb-design-reconstruction-agent`, `kicad-schematic-build-agent`, `kicad-pcb-layout-agent`, `kicad-design-review-agent`, with Stage/Owner/Consumes/Produces/Gate/`INCOMPLETE` columns |
| 3.6 | PASS | `grep -c "dossier\|design_brief" review-map-schema.md` = 13 (≥2); `:162-163` are the two "inside the hash — when present, whole" rows |
| 4.1 | PASS | frontmatter `tools:` = `mcp__konnect__*`, `Read`; `skills:` = exactly `konnect`, `kicad-photo-intake`, `kicad-board-dossier`; Phase 3 "Comprehension — the dossier" at `:94`; Hard rules 6 (`Read` confined to `source_images` + `views/`) and 7 (no claim without `{view, rect_px}` evidence) |
| 4.2 | PASS | file exists; `skills:` = only `konnect`, `kicad-design-reconstruction`; no `Read` in `tools:`; Hard rule 2: "From the `library` toolset you call `search_symbols` and `search_footprints`, and nothing else"; Phase 7 returns the five-field block |
| 4.3 | PASS | `grep -c "kicad-photo-to-board\|pcb-design-reconstruction-agent" konnect/SKILL.md` = 3 (≥2); `:71` is the rewritten "I have photos of a board" row, `:110` the "Agent Routing and Mutation Ownership" section |
| 4.4 | PASS | `grep -c "Building from an approved design brief"` = 1 (`:72`) |
| 4.5 | PASS | `grep -c "Building from an approved design brief"` = 1 |
| 4.6 | PASS | `cargo test -p konnect --test asset_references` → 12 passed, 0 failed, incl. all five named checks |
| 5.1 | PASS | `manifest.rs:132-137` (`kicad-board-dossier` + `dossier-schema.md`), `:140-146` (`kicad-design-reconstruction` + `design-brief-schema.md`), `:150-151` (`kicad-photo-to-board`), `:176-177` (`pcb-design-reconstruction-agent.md`) |
| 5.2 | PASS | `backticked_tool_names_in_prose_exist_in_the_registry` ok; the added block is one commented run at `asset_references.rs:769-857`; `image_path`/`project_dir`/`map_id` are absent from it, proven by `not_tools_allowlist` 3/3 ok, which parses the block out of `include_str!("asset_references.rs")` rather than copying it |
| 5.3 | PASS | `grep -rn "231\|238" docs/TROUBLESHOOTING.md packaging/metadata.json plugin/plugin.json` → exit 1, no match; `232`/`239` present at `metadata.json:4`, `plugin.json:4`, `TROUBLESHOOTING.md:369` |
| 5.4 | PASS | `cargo test -p konnect --test doc_tool_counts` → 6 passed incl. `tool_directory_lists_every_registered_tool`; `grep -c prepare_board_photo tool-directory.md` = 1; header `:390 ### \`photo_intake\` · 6 tools` |
| 5.5 | PASS | `docs/PHOTO_TO_BOARD_WORKFLOW.md` exists (9 835 B); `README.md:313` links it, in the external-dependency note beside the retrace paragraph (`:310`) |
| 6.1 | PASS (by handoff) | `05-orchestrator-checklist-6.1.md` scored 7/8 PASS, item 4 substantive PASS. Claimed artifacts verified on disk: `…/b3d0be59-…/review_map.json` has `approved: true` and a `dossier` with all 10 D4 sections; `views/` holds exactly 12 PNGs. Spot-checks: item 1 `identity.summary` quotes `"SEMAFORO L3 24V 03/2020"`, `evidence[0].view = A_bottom_silk.png` exists at 920x340 with `rect_px [452,606,152,26]` inside it; item 2 three `24V` markings `basis: observed`; item 5 exactly 4 `plated/tinned` corner holes each with `position_px` (+2 unplated field holes, separate); item 6 one 2-pole screw terminal, bottom edge; item 7 12 hypotheses carry a non-empty `calculation` + `assumptions`; item 8 `board_size_mm: null`, `scale_status` "UNRESOLVED", `OQ-4` in `open_questions`, `scale_reference` has no `mm_per_px` |
| 7.1 | PASS | `cargo fmt --check` → exit 0 (re-run with both new files) |
| 7.2 | PASS | `cargo clippy --all-targets` → exit 0, zero `^warning`/`^error` lines |
| 7.3 | PASS | `cargo test -p konnect-core` exit 0 (1384 lib + 4 + 12 + 5 e2e + **6** + **3** new, 16 ignored); `cargo test -p konnect` exit 0 (15 binaries, 0 failed) |

### Spec-delta scenarios (23)

| Requirement / scenario | Verdict | Evidence |
|---|---|---|
| board photo views · a cropped, scaled view is produced | PASS | `a_cropped_and_scaled_view_is_saved_at_exactly_the_size_it_reports`; mine returns `view_path` under `…/<map_id>/views/oriented_corner.png`, `source_rect_px [42,2,16,16]`, `output_size_px [32,32]` |
| … · a view carries a physical scale when one is resolved | PASS | `mm_per_px_is_reported_only_when_the_saved_map_resolved_one`; my `mm_per_px_appears_only_after_the_saved_map_resolves_one` walks all three states through `save_photo_review_map` → no map / unresolved / resolved `0.125` |
| … · an out-of-bounds crop is rejected | PASS | `an_out_of_bounds_crop_names_the_oriented_dimensions_and_writes_nothing`; mine asserts the message contains `60x120` (the **oriented** size) and the `views/` fingerprint is unchanged |
| … · a map directory that does not exist is rejected | PASS | `a_map_id_no_scan_ever_assigned_is_rejected_and_mints_nothing`; mine also asserts `…/photo_intake/never-scanned` does not exist afterwards |
| board dossier · a dossier persists like any other reviewed section | PASS | `a_map_with_both_new_sections_round_trips_through_save_and_load`; my checkpoint test asserts `loaded["map"]["dossier"] == sample_dossier()` after a full save/approve cycle |
| … · an inferred topology claim states its calculation and assumptions | PASS (prose) | `kicad-board-dossier/SKILL.md:259-263`: "**Every numeric hypothesis states its `calculation` and its `assumptions`.** The calculation is the arithmetic written out with units, not its result. The assumptions list every value *you* supplied that the board did not" |
| … · a count method disagreement is recorded, not silently resolved | PASS (prose) | same file `:156-162`: "When a second method gives a different number for the **whole class**, record it in `count_alternatives` … **Two methods disagreeing is itself evidence.** Collapsing it into the trusted number alone throws away the only signal a reviewer has" |
| … · an unresolved question keeps competing hypotheses, not one guess | PASS (prose) | same file `:264-267`: "**Competing answers stay as separate hypotheses**, each with its own `confidence`. You never pick one and delete the other, never average two confidences into a third"; agent Hard rule 8 repeats it |
| … · adding a dossier to a map changes its content hash | PASS | `adding_a_dossier_to_an_approved_map_revokes_its_approval` (hash differs, `approved` → false, `approved_at`/`content_hash_at_approval` → null); my contract test repeats it through the router |
| design brief · a design brief persists additively | PASS | `a_map_with_both_new_sections_round_trips_through_save_and_load`; mine asserts `loaded["map"]["design_brief"] == sample_design_brief()` alongside an already-approved `dossier` |
| … · design reconstruction is refused before the dossier is approved | PASS (prose) | `pcb-design-reconstruction-agent.md:51-60` Phase 0: "If `approval_valid` is false, **stop** … Do not write `design_brief` content for an unapproved dossier — not a draft, not a partial one, not 'ready for when they approve'"; Hard rule 1 repeats it |
| … · every BOM entry names a real library part | PASS (prose) | `kicad-design-reconstruction/SKILL.md:148-152`: ids are "the strings the search returned — copied, not retyped from memory"; unresolved ⇒ both ids `null`, `resolution_status: "unresolved"`, `candidates[]`; `:231` restates it as a rule |
| handoff · schematic build places one symbol per BOM entry | PASS (prose) | `kicad-schematic-build-agent.md:90-93`: "Place one real library symbol per `bom` entry whose `resolution_status` is `resolved`, matched via `search_symbols` starting from that entry's own `kicad_symbol` lib_id. The brief's id is the search term, not the licence to skip the search." |
| … · PCB layout treats physical constraints as hard limits | PASS (prose) | `kicad-pcb-layout-agent.md` step 3 maps all ten `layout-methodology.md` section-1 rows to `physical_constraints` fields; step 4: "Treat those values as **hard** constraints, not suggestions: the board size is the outline you create, the mounting-hole positions are fixed placements"; step 5 forces `INCOMPLETE` on `board_size_mm` or a load-bearing unresolved row |
| … · an unapproved design brief cannot reach build | PASS (prose) | both consumer sections, step 2 each: schematic — "Proceed **only when the response's `approval_valid` is true** … When it is false, stop and report `INCOMPLETE`"; layout — "never the map's own `approved` field … When it is false, place nothing and report `INCOMPLETE`". See Deferred finding 1: no executable guard pins either line |
| review map · a freshly scanned map persists as editable JSON | PASS | `a_freshly_scanned_map_persists_as_editable_json` ok |
| … · low-confidence components are flagged, never auto-corrected | PASS | `low_confidence_components_are_flagged_never_auto_corrected` ok |
| … · the review map survives across sessions | PASS | `the_review_map_survives_across_sessions_with_hand_edits_intact` ok; `out_of_schema_keys_survive_a_save_and_load_round_trip` (e2e, through the router) ok |
| … · agent-resolved scale reference is recorded with evidence | PASS | schema carries `mm_per_px`+`evidence`; my `mm_per_px_appears_only_after_the_saved_map_resolves_one` round-trips both through save→load. Prose half, `kicad-board-dossier/SKILL.md:224-230`: "write **both** `mm_per_px` and `evidence` … they are always a pair, and a millimeter figure you cannot name evidence for must not be written at all" |
| gate · unapproved map cannot reach schematic build | PASS | `load_reports_approval_valid_false_after_an_out_of_band_edit` ok (named by the scenario itself); `kicad-schematic-build-agent.md`'s pre-existing "Building from an approved photo-intake map" section, guarded by `agents_make_claimed_evidence_executable`'s `approval_valid`/`INCOMPLETE` markers |
| … · approval requires an explicit call | PASS | `save_alone_never_approves`, `approve_records_the_hash_and_a_timestamp` ok |
| … · editing an approved map revokes approval | PASS | `editing_an_approved_map_revokes_approval_and_undo_does_not_restore_it` ok |
| … · adding a design brief revokes a dossier-only approval | PASS | `adding_a_design_brief_revokes_a_re_approved_dossier_only_map` ok; my `both_checkpoints_revoke_on_content_and_reopen_only_on_an_explicit_approval` walks save→approve→dossier→false→approve→true→brief→false→approve→**true**→drop dossier→false through the router |

## Evidence

1. **Four gates, re-run with my two new files in the tree** (bash, `PROTOC` +
   VS 2022 CMake on `PATH`): `cargo fmt --check` exit 0 · `cargo clippy
   --all-targets` exit 0, no `^warning` line · `cargo test -p konnect-core`
   exit 0 → `1384 passed; 0 failed; 16 ignored` (lib), `4/0`, `12/0`, `5/0 1
   ignored` (e2e), `6/0` (`prepare_board_photo_contract`), `3/0`
   (`two_checkpoint_approval_contract`) · `cargo test -p konnect` exit 0, 15
   binaries. Live, with `RETRACE_PYTHON` and `-- --ignored`:
   `scan_pcb_photo_runs_a_real_retrace_scan` ok and
   `a_real_scan_feeds_the_gate_and_leaves_the_home_directory_alone` ok.
2. **Must-check (b), through the published contract.** No existing test of
   `prepare_board_photo` met the validator — the six unit tests all call
   `handle_prepare_board_photo` directly (`photo_intake.rs:4513`), and
   `photo_intake_gate_e2e.rs` never mentions the tool (`grep` exit 1). New
   `crates/konnect-core/tests/prepare_board_photo_contract.rs` does
   `ToolRouter::load("photo_intake")` → `def.input_validator.validate` →
   `def.handler`, on an in-test JPEG carrying a spliced `Exif\0\0` APP1
   orientation-6 segment. Results: the nested `crop` object survives
   `close_input_schema` (the `save_photo_review_map` defect class does **not**
   recur); crop is measured post-orientation (`source_size_px [60,120]`, the
   crop returns all-red where a stored-space crop returns black); an
   out-of-bounds crop is a handler error naming `60x120`; **`scale` outside
   `0.25..=4.0` and a non-token `label` are rejected by the validator, before
   the handler runs** — the published schema carries `minimum`/`maximum` and
   `pattern`, so the handler's own rejection is a second layer, asserted too;
   `mm_per_px` appears only after a `save_photo_review_map` that resolved one.
3. **Must-check (c), both checkpoints.** The existing tests stop at
   `approval_valid: false`; nothing showed the gate re-opening for a
   `design_brief`, so a gate that only ever revoked would have been green. New
   `two_checkpoint_approval_contract.rs` walks the whole sequence through the
   router and asserts `true` after each `approve_photo_review_map`, `false`
   after the dossier is dropped, and that a map with neither section
   serializes no `dossier`/`design_brief` key and keeps its approval across an
   identical resave (the `skip_serializing_if` half of the pinned digest,
   whose other half is `the_content_hash_of_the_fixture_map_is_pinned`,
   byte-identical to base). `"dossier": null` is refused **at the validator**
   through the contract — one layer earlier than
   `save_rejects_a_new_section_that_is_present_but_not_an_object` sees it — so
   both layers are asserted and the record is still `approval_valid: true`
   afterwards.
4. **"Wrote nothing" assertions are dirty-fail-safe.** Every no-write claim
   uses one `fingerprint()` over the same directory, and
   `a_jpeg_crop_rotate_and_scale_survives_the_published_schema` asserts
   `assert_ne!(before, fingerprint(…))` on the success path with that same
   function — so the unchanged-fingerprint assertions cannot pass vacuously.
5. **Must-checks (d)–(g).** (d) `router/mod.rs:222-231` holds exactly the six
   D7 paths, no seventh, and the test is green. (e) `asset_references` 12/12,
   `doc_tool_counts` 6/6, `not_tools_allowlist` 3/3 (it parses the allowlist
   from `include_str!("asset_references.rs")`, so the 89 new entries were
   re-checked, not trusted), `schema_parameter_usage` 5/5 — there is no
   `install` test target in `konnect`; the installation checks live in
   `protocol_stdio.rs` (`installation_info_*`, 18/18 green). (g)
   `dossier-schema.md:132-154` gives `locations[]` as
   `{region, view, rect_px, count, kind}` with "the class's `count` **equals
   the sum of `count`** over `observation` entries" and `cross_check`
   "**excluded from the sum**"; `:95` requires a hole `role` to "literally
   contain `plated` or `unplated`"; `:105` requires a connector `edge` to
   "start with `top`, `bottom`, `left` or `right`".

## For the next agent

1. Verdict is DONE on criteria and scenarios. `08-reviewer.md` independently
   returned **FIX** on a *guard* layer, not on a criterion — its SERIOUS item
   is Deferred finding 1 below. Nothing I ran contradicts the reviewer; we
   scored different questions. The orchestrator decides whether a guard gap
   that no criterion names blocks the archive.
2. My two new files are committed as `bab650d` on
   `orc/board-dossier-reconstruction`, **not pushed**. They are additive test
   files only; no existing file was edited at any point this round.
3. If a fix round happens, re-run only: `cargo test -p konnect --test
   asset_references`, `cargo test -p konnect-core --test
   prepare_board_photo_contract`, `--test two_checkpoint_approval_contract`,
   plus `cargo fmt --check`. A cold full `-p konnect-core` run is ~50 s of
   test time behind several minutes of build; chain the cargos, never two at
   once.
4. The `image` crate's JPEG encoder writes no EXIF, so an orientation fixture
   has to splice its own APP1 segment (`jpeg_with_exif_orientation`,
   `prepare_board_photo_contract.rs:150`). JPEG is lossy: assert red-vs-black
   with a tolerance and crop clear of the 8x8 block boundary, never exact
   `[255,0,0]`.

## Deferred findings

1. **No executable guard pins the two new gate promises**
   (`crates/konnect/tests/asset_references.rs:177-197`).
   `agents_make_claimed_evidence_executable`'s `cases` array names only
   `kicad-schematic-build-agent.md` and `kicad-design-review-agent.md`. The
   `approval_valid` / `INCOMPLETE` / "place nothing" lines in
   `kicad-pcb-layout-agent.md`'s and `pcb-design-reconstruction-agent.md`'s new
   sections — the text the spec scenario "an unapproved design brief cannot
   reach build" names as its verification — can be deleted with a fully green
   suite. The sibling line in `kicad-schematic-build-agent.md:79-83` *is*
   pinned, by the markers at `asset_references.rs:188-189`. Remedy is two
   entries in that existing array. Raised first as `04-developer.md` deferred
   finding 4 and as `08-reviewer.md`'s SERIOUS; I score the scenario PASS
   because the spec says "verified by their text" and the text is there and
   says it, and record this so the prose cannot rot silently.
2. **`README.md:345` is stale**: "6 skills + 2 agents bundled", while
   `manifest.rs` ships 10 skills (`SKILLS`, `:33`) and 5 agents (`AGENTS`,
   `:158`). Already wrong at 7+4 before this change, and `doc_tool_counts.rs`
   sweeps tool counts only, so nothing catches it. Not named by any criterion
   (5.3 covers `TROUBLESHOOTING.md`/`metadata.json`/`plugin.json`; 5.5 covers
   only the workflow link). A `manifest.rs`-derived assertion in
   `doc_tool_counts.rs` would close it permanently.
3. **The task 6.1 artifact predates the fix it triggered.** The dossier at
   `…/b3d0be59-…/review_map.json` was produced at `b2eda51`; its
   `component_survey[].locations[]` entries carry only
   `{rect_px, region, view}` — no `count`, no `kind` — which is exactly the
   machine-reconciliation half the orchestrator scored FAIL on item 4. The fix
   landed at the doc layer (`a099d9e`, `dossier-schema.md:132-154`) and was
   never re-run against the photos, so no artifact demonstrates the reconciled
   shape. Also: the agent read the silkscreen as `SEMAFORO L3` where design
   D12 and the prototype say `SEMAFARO 1.3`, and counted 119 LEDs on board A
   against D12's 97-118 window — both recorded by the runner as the new
   reference, which is what task 6.1's acceptance directs, but D12's literals
   in `design.md` still say otherwise.
4. **Four spec scenarios are prose-only by design and one is silently so.**
   "an inferred topology claim states its calculation", "a count method
   disagreement is recorded", "an unresolved question keeps competing
   hypotheses" and "every BOM entry names a real library part" all say "no
   automated test enforces this" in the spec itself — honest. "an unapproved
   design brief cannot reach build" does **not** carry that caveat while its
   two neighbours in the same requirement do, which reads as an oversight in
   the spec rather than a deliberate exemption (see finding 1).
