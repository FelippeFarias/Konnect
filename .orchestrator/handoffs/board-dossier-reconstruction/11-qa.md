---
change: board-dossier-reconstruction
task: VERIFY round 2 focused regression
agent: qa
verdict: DONE
failing_layer: n/a
---

## Result

1. **(a) Four gates + both live tests: PASS.** `cargo fmt --check` exit 0;
   `cargo clippy --all-targets` exit 0, zero `^warning` lines; `cargo test -p
   konnect-core` exit 0 (1385 lib + 8 integration suites, 0 failed, 16+1
   ignored as expected); `cargo test -p konnect` exit 0 (15 binaries, 0
   failed). Both live retrace tests green under `RETRACE_PYTHON`.
2. **(b) Spec-cited guard tests: PASS.** `agents_make_claimed_evidence_executable`
   and `skills_define_the_same_evidence_boundary_as_their_agents` both green
   in isolation; the markers `specs/photo-intake/spec.md` cites are present
   in the cases for the named files. See Evidence 2 for the one imprecision
   worth flagging, filed as a deferred finding rather than a FAIL.
3. **(c) Reused-label refusal through the published contract: PASS.** The
   existing `a_reused_label_is_refused_and_the_original_view_is_unchanged`
   (calls the handler directly) still passes; the contract suite had no test
   driving this through `ToolRouter`, so I added ONE new file,
   `crates/konnect-core/tests/prepare_board_photo_reuse_contract.rs`
   (committed `df870a1`, not pushed), which does. Both pass.
4. **(d) Round-1 regression sweep: PASS, no regressions.** The four
   fix-touched areas (asset guards, README text, `prepare_board_photo`
   tests, dossier skill) all re-verified green; see Evidence 4.
5. Worktree HEAD after my addition: `df870a1` (on top of `f7c329c`),
   `git status --short` clean except that one new file before I committed
   it. Verdict **DONE**: all four must-checks PASS, nothing to send back to
   `dev`.

## Evidence

1. **Command evidence for (a).** From the worktree, with `PROTOC` and the VS
   2022 CMake `bin` on `PATH`: `cargo fmt --check` → `EXIT_FMT=0`. `cargo
   clippy --all-targets` → `EXIT_CLIPPY=0`, `grep -c '^warning'` over the
   full log = 0. `cargo test -p konnect-core` → `EXIT_TEST_CORE=0`, `1385
   passed; 0 failed; 16 ignored` (lib) plus `4/0`, `12/0`, `5/0 1 ignored`
   (e2e), `6/0` (`prepare_board_photo_contract`), `1/0`
   (`prepare_board_photo_reuse_contract`), `3/0`
   (`two_checkpoint_approval_contract`). `cargo test -p konnect` →
   `EXIT_TEST_KONNECT=0`, 15 binaries incl. `asset_references` 12/12.
   Live: `cargo test -p konnect-core --lib
   tools::photo_intake::live_retrace_tests::scan_pcb_photo_runs_a_real_retrace_scan
   -- --ignored --exact` → `1 passed` (the first attempt used a bare test
   name and matched 0 tests under `--exact`; the fully-qualified module path
   fixed that — no code issue). `cargo test -p konnect-core --test
   photo_intake_gate_e2e a_real_scan_feeds_the_gate_and_leaves_the_home_directory_alone
   -- --ignored --exact` → `1 passed`.
2. **Command evidence for (b), plus the marker-distribution note.** `cargo
   test -p konnect --test asset_references agents_make_claimed_evidence_executable
   -- --exact` → `1 passed`; `... skills_define_the_same_evidence_boundary_as_their_agents
   -- --exact` → `1 passed`. Read `asset_references.rs:177-216` directly:
   `pcb-design-reconstruction-agent.md`'s case carries `approval_valid`
   (cited by "design reconstruction is refused before the dossier is
   approved") and `kicad-schematic-build-agent.md` / `kicad-pcb-layout-agent.md`
   carry `approval_valid`+`INCOMPLETE` (`kicad-pcb-layout-agent.md` also
   `load_photo_review_map`), matching "an unapproved design brief cannot
   reach build". The skills case (`:246-301`) covers the three new skills,
   but the spec's own phrase "pin the `load_photo_review_map`,
   `approval_valid`, and `INCOMPLETE` markers **in each file's text**" reads
   as per-file when it is really per-group: `kicad-board-dossier/SKILL.md`'s
   case pins only `INCOMPLETE` of the three (not `approval_valid` or
   `load_photo_review_map` — that skill doesn't consume the design-brief
   gate), and `kicad-photo-to-board/SKILL.md`'s case pins `approval_valid`+
   `INCOMPLETE` but `approve_photo_review_map` in place of
   `load_photo_review_map`. This is a spec wording imprecision, not a guard
   gap — the two SERIOUS-finding files (agent cases) and all three skill
   files do have a case with `INCOMPLETE` pinned at minimum — filed as
   Deferred finding 1.
3. **Command evidence for (c).** New file
   `crates/konnect-core/tests/prepare_board_photo_reuse_contract.rs`: loads
   `photo_intake` via `ToolRouter::load`, validates against the published
   schema, calls `prepare_board_photo` once under `label: "reused_label"`,
   reads the saved view's bytes, calls it again with a different source
   image and crop under the same label, asserts `is_error` and that the
   message contains the first call's `view_path`, then re-reads the file and
   asserts `original_bytes == bytes_after`. A third call under a fresh label
   in the same map still succeeds, proving the refusal is scoped to the
   label, not the map. `cargo test -p konnect-core --test
   prepare_board_photo_reuse_contract` → `1 passed; 0 failed`. `cargo fmt
   --package konnect-core` was run after authoring and after trimming two
   clippy-flagged dead-code helpers (`views_dir`, `Fixture::views`) that this
   leaner test didn't end up needing. Committed `df870a1`
   (`test(photo-intake): …`), `git status --short` clean afterward.
4. **Command evidence for (d).** Asset guards: `asset_references` 12/12
   (Evidence 2). README text: `grep -n "skills + " README.md` →
   `345:| Skills / agents | — | 10 skills + 5 agents bundled |`; cross-checked
   against `manifest.rs` — 10 `SkillManifest { … }` instances at
   `:34-149` and 5 `AgentManifest { … }` instances at `:159-175` (a bare
   `grep -c "SkillManifest {"` over-counts to 16 because it also matches
   inside `HookSkillManifest {`; counted instances by line, not by
   substring). `prepare_board_photo` tests: the pre-existing 6-test
   `prepare_board_photo_contract.rs` suite is unchanged and green (6/0,
   Evidence 1), and the internal
   `tools::photo_intake::board_view_tests::a_reused_label_is_refused_and_the_original_view_is_unchanged`
   is green as part of the 1385-test lib run. Dossier skill:
   `kicad-board-dossier/SKILL.md:313` now reads `"The silkscreen reads
   `SEMAFORO L3 24V 03/2020`"` (the 6.1 run's actual reading, per commit
   `f7c329c`), and its `skills_define_the_same_evidence_boundary_as_their_agents`
   case is part of the green 12/12.
5. **One operational note, not a defect.** A first pass at the full gate
   chain hit `error: extern location for konnect_core does not exist` in
   `konnect`'s doctest step; `tasklist` at that moment showed a second,
   unrelated `cargo.exe`/three `rustc.exe` processes active in this shared
   worktree. Re-running the full chain after confirming no concurrent cargo
   process (clean `tasklist`) reproduced cleanly end to end — see Evidence 1.
   Treat any single `extern location … does not exist` failure in this
   worktree as contention, not a regression, and re-run after checking
   `tasklist` first.

## For the next agent

1. Nothing to fix. All four must-checks PASS; the round-1 table (`07-qa.md`)
   has no regression.
2. My one new file, `crates/konnect-core/tests/prepare_board_photo_reuse_contract.rs`,
   is committed as `df870a1` on `orc/board-dossier-reconstruction`, **not
   pushed**. It duplicates `prepare_board_photo_contract.rs`'s private test
   harness (that file's helpers are not `pub`) rather than editing it.
3. This worktree is shared: check `tasklist` for `cargo.exe`/`rustc.exe`
   before starting a gate run, and re-run once cleanly if a build step fails
   with an `extern location … does not exist` error — see Evidence 5.
4. `--ignored --exact` against `konnect-core`'s lib test binary needs the
   fully-qualified module path
   (`tools::photo_intake::live_retrace_tests::scan_pcb_photo_runs_a_real_retrace_scan`),
   not the bare function name, or it silently matches 0 tests.
5. `08-reviewer.md`'s deferred findings 1-3 and 5, and `07-qa.md`'s deferred
   finding 3, were out of this round's four-finding scope and were not
   re-checked here; they still stand as recorded there.

## Deferred findings

1. `specs/photo-intake/spec.md:198-204` says the skills case pins
   `load_photo_review_map`/`approval_valid`/`INCOMPLETE` "in each file's
   text", but `kicad-board-dossier/SKILL.md`'s case only pins `INCOMPLETE`
   and `kicad-photo-to-board/SKILL.md`'s pins `approve_photo_review_map` in
   place of `load_photo_review_map`. The property that matters (a guard now
   exists for all three new skills and both previously-uncovered agent
   files) holds; the sentence over-states which marker lands in which file.
   A one-clause fix in the scenario's THEN naming the markers per file, or
   softening "in each file's text" to "across these cases", would close it.
2. `08-reviewer.md` deferred findings 1-3 (no null-rejection on
   `scale_reference.mm_per_px`/`.evidence`, no check that a
   `dossier.evidence[].view` names a real file, no test for a `"dossier":
   {}` section) and 5 (two hard-coded marker-list tests could fold into one
   data-driven table) were not in this round's scope and still stand.
3. `07-qa.md` deferred finding 3 (the task 6.1 artifact predates the fix it
   triggered — no re-run against the photos with the reconciled
   `count`/`kind` shape) still stands; nothing in this round touched it.
