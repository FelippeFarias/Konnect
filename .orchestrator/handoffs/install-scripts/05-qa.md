---
change: install-scripts
task: re-verify scripts/install.ps1 + scripts/install.sh at 334a8ac (qa R2, reviewer items 1-9, round-1 regressions) without touching the real install
agent: qa
verdict: DONE
---

## Result

1. Verdict DONE. Every row below passes in both scripts. My round-1 FIX (R2) and the reviewer's SERIOUS 1 plus MINORs 2-10 are closed, and none of the 11 round-1 PASS requirements regressed.
2. The HARD TEST RULE held. Every invocation carried `-DryRun`/`--dry-run`, or all of a temp `-Target` + `-SkipInit` + `-NoRetrace`, through wrappers that append those flags. Before the first real call I proved the ps1 wrapper refuses both `C:\Users\felip\.konnect\bin\konnect.exe` and the bare temp root. There was one authorized exception per script, for "init fails after the swap". The real exe sha256 and RETRACE_PYTHON are identical before and after (Evidence 0).
3. Tested bytes: `git show 334a8ac:scripts/install.{ps1,sh}` copied into a temp fake repo. The blob sha256 equals the copy's: ps1 `d937f3f4a6cec1b3…`, sh `531a47ca3a6c383c…` (LF). The PS 5.1 parser reports 0 errors and `bash -n` is clean. The worktree is untouched and clean at `334a8ac`.

| Check | ps1 | sh |
|---|---|---|
| (a) R2: `-WhatIf`, `-DryRum`, `-Bogus`, stray word rejected before any action | PASS | PASS (exit 2) |
| 1 Directory target refused; target left in place after the rename → stop before the copy | PASS | PASS |
| 2 Failure after the rename prints the rollback (copy/restore, hash mismatch, unexpected error, **init failure**) | PASS | PASS |
| 3 Failed restore reported as a failure, with working restore lines | PASS | PASS |
| 4 Rollback safe to paste twice, including a `'` in the path | PASS | PASS |
| 5 Staleness: RELIABILITY_CONTRACT.md, Cargo.toml, rust-toolchain.toml, CARGO_TARGET_DIR | PASS | PASS |
| 6 Project-entry shadow warning | PASS | PASS |
| 7 ps1 exact-case name, POSIX path, relative `-ClaudeConfig` (+ driveless refused) | PASS | n/a |
| 8 sh invalid JSON stops (jq, jq-1.6 exit-2 shim, python); no reader → falls back | n/a | PASS |
| 9 Messages: Help, persistent PROTOC hint, fresh-install undo, RETRACE restart text | PASS | PASS |
| R1 Options, `-Help`, contradictions, `-Client foo` | PASS | PASS |
| R3 Build decision | PASS | PASS |
| R4 PROTOC/CMake pre-check; cargo invocation | PASS | PASS |
| R5 Discovery fixtures | PASS | PASS |
| R6 Identical → skip | PASS | PASS |
| R7 Dated `.bak`, collision suffix | PASS | PASS |
| R8 Copy failure → renamed back | PASS | PASS |
| R9 init/status plan (live success path: see Evidence 13) | PASS | PASS |
| R10 RETRACE_PYTHON detection and plan (dry) | PASS | PASS |
| R11 Dry run changes nothing | PASS | PASS |
| R12 Summary and rollback restore exactly | PASS | PASS |

## Evidence

`<S>` = session scratchpad `…\scratchpad\qa05` (removed at the end). Fakes were built with rustc and write only to `<S>\logs`. `fake-old` (`konnect 0.11.0-old`, sha `4dceee34`) and `fake-new` (`0.99.0-new`, sha `1cfd521e`) answer `--version`, and any other argument prints `refused (writes nothing)` and exits 3. The fake `cargo.exe` logs args, cwd, PROTOC and PATH[0], then exits with `FAKE_CARGO_EXIT`.

0. Safety snapshot. Before (10:36:59) and after (10:49:33), identical on every line:
   - `exe sha256: DEA8824B1357F6587FA98792D8246C6516CBD5A12F30ECEDB52F31B7A447DEC5`
   - `mtime: 2026-09-21T18:30:27.9145510Z`, `size: 34333696`
   - `bin dir: konnect-0.12.0-2026-09-17.exe.bak, konnect.exe, schematic-viewer.exe`
   - `RETRACE_PYTHON(user): []`, `RETRACE_PYTHON(machine): []`
   - `settings.json mtime: 2026-09-22T13:16:45.9643147Z` (Claude Code's own 10:16 change, already in the log)
   - `skills`/`agents` dir mtimes `2026-09-17T23:31:14Z`
   - After only: `konnect --version: konnect 0.12.0`.

   Disclosure: some ps1 dry runs fell back to the default target, and so ran the real exe's `--version` and hashed it (read-only, allowed under `-DryRun`). These were the no-konnect fixtures (`none`, `missing`, `empty`, `capital`) and 16 runs where my fixture path was mangled.
1. (a) Temp target holding fake-old, plus `-SkipBuild`, `-Target <S>\t\a\konnect.exe -SkipInit -NoRetrace`:
   - `-WhatIf`, `-DryRum`, `-Bogus` → `A parameter cannot be found that matches parameter name 'WhatIf'` (and `'DryRum'`, `'Bogus'`), `EXIT=1`.
   - `stray` → `A positional parameter cannot be found that accepts argument`, `EXIT=1`.
   - Target-dir fingerprint `5b7e1d7ad860` before and after every probe; only `konnect.exe 4dceee34` remains.
   - `-Dry` still binds to DryRun (`would rename …`, EXIT=0). `-Help` EXIT=0. `-Client foo` → `Cannot validate argument on parameter 'Client'` EXIT=1. `-SkipBuild -Rebuild` → `contradict` EXIT=1. `-RetracePython x -NoRetrace` → `contradict` EXIT=1.
   - sh: `--whatif`, `--dryrum`, `stray` → `Unknown argument: …` EXIT=2. The sh contradiction checks and `--client foo` → EXIT=1.
2. Item 1.
   - Folder as target, holding an `OLD-SENTINEL` konnect.exe: ps1 `the install target <S>\t\b2p is a folder. Pass the full path to the .exe, e.g. -Target '<S>\t\b2p\konnect.exe'.` EXIT=1. sh gives the same text (`…konnect.exe file, e.g. --target '…'`) EXIT=1, and a trailing `/` is also refused. The sentinel (`099000de`) is unchanged in both.
   - Rename leaves the target in place (ps1 `Move-Item` shadowed to copy; sh `mv` shim that copies): `…konnect.exe still exists after renaming it to …-104102.exe.bak; stopping before the copy so nothing is overwritten.` EXIT=1, rollback printed, target still the old sha.
3. Items 2-3, injected failures, both scripts. Each case exits 1 with a correct message, and the final state is correct after the pasted rollback:

   | Case | ps1 | sh | State afterwards |
   |---|---|---|---|
   | Copy fails, restore works | `copy … failed (injected copy failure); the previous binary was renamed back - nothing changed.` | same text | only `konnect.exe 4dceee34` |
   | Copy fails and restore fails | `…and putting the previous binary back failed too (injected restore failure). The previous binary is safe at …-104105.exe.bak; the lines below restore it.` plus one Move-Item | same, one guarded `mv` | paste restores `konnect.exe 4dceee34` |
   | Hash mismatch after the copy | `does not match the build after copying.` plus two Move-Item lines | same, one guarded line | paste restores |
   | Unexpected error (ps1 `Get-FileHash` throws on call 4; sh `export -f cp` exits 7) | `unexpected error: injected hash failure (at …)` plus rollback | `stopped unexpectedly (exit 7)`, EXIT=7, plus rollback | paste restores |

   In every case the second paste changes nothing. ps1 gets `Cannot create a file when that file already exists` / `Cannot move item because the item at …`. sh gets `rollback stopped: already done, or the files were moved - check the folder`.
4. SC2, init fails after the swap (authorized exception, one run per script): `-Target <S>\t\xp\konnect.exe -SkipBuild -NoRetrace`, `CARGO_TARGET_DIR=<S>\ctd` whose `release\konnect.exe` is fake-new (init exits 3), with no `-SkipInit`. ps1 output:
   ```
   CARGO_TARGET_DIR = <S>\ctd
   renamed <S>\t\xp\konnect.exe -> <S>\t\xp\konnect-0.11.0-old-20260922-104209.exe.bak
   copied <S>\ctd\release\konnect.exe -> <S>\t\xp\konnect.exe
   running: "<S>\t\xp\konnect.exe" init
     fake konnect 0.99.0-new: 'init' refused (writes nothing)
   install: ERROR: konnect init failed (exit 3).
   == Stopped - changes made before the error
     - Changed <S>\t\xp\konnect.exe (old binary 0.11.0-old renamed to …-104209.exe.bak)
     to undo, paste these lines into PowerShell (safe to paste twice):
         Move-Item -LiteralPath '<S>\t\xp\konnect.exe' -Destination '…-0.99.0-new-20260922-104209-rolledback.exe.bak'
         Move-Item -LiteralPath '…-0.11.0-old-20260922-104209.exe.bak' -Destination '<S>\t\xp\konnect.exe'
         & '<S>\t\xp\konnect.exe' init
   EXIT=1
   ```
   - The fake log shows only `--version` ×2 and `init`, all inside `<S>`.
   - Pasting the two Move-Item lines (never the `init` line) twice → `konnect.exe 4dceee34` + `-rolledback 1cfd521e`. The second paste errors and changes nothing.
   - sh gives the same shape: `konnect init failed (exit 3).`, EXIT=1, one guarded `mv` line plus `'<S>/t/xs/konnect.exe' init`. Paste 1 restores the old sha; paste 2 → `rollback stopped: already done…`.
5. Item 4, target `<S>\t\apos*\O'Brien\bin\konnect.exe` (real runs).
   - ps1 prints `Move-Item -LiteralPath '…\O''Brien\bin\konnect.exe' …`.
   - sh prints `[ ! -e '…/O'\''Brien/…rolledback.exe.bak' ] && … mv -- …`.
   - Each was pasted twice. Both folders end as `konnect.exe 4dceee34` + `…-rolledback.exe.bak 1cfd521e`, identical after paste 1 and paste 2. This also re-proves R12: the rollback restores exactly.
6. Item 5 + R3 (dry runs; all tracked inputs set to 2026-01-01, build to 2026-02-01, then one file touched).
   - Touching each of `docs/RELIABILITY_CONTRACT.md`, `Cargo.toml`, `rust-toolchain.toml`, `crates/konnect/src/manifest.rs`, `Cargo.lock` gives `build needed: build (2026-02-01 00:00:00) is older than <that file> (…)` in the ps1, and `build is older than <that file>` in the sh.
   - Baseline and a newer UNTRACKED `crates/untracked.rs` → `up to date`. `-Rebuild` → `-Rebuild given`. No build → `no build at …`.
   - `-SkipBuild` on a stale build → `WARNING: -SkipBuild: installing a stale build`, EXIT=0. `-SkipBuild` with no build → EXIT=1.
   - `CARGO_TARGET_DIR=ctdrel` (relative) → `CARGO_TARGET_DIR = <S>\repo\ctdrel`, `up to date: …\ctdrel\release\konnect.exe`, `new build version: 0.11.0-old`.
   - Absolute `<S>\ctd` → `0.99.0-new`. A missing dir → `no build at <S>\nope\release\konnect.exe`. Same results in both scripts.
7. R4 (real runs, temp target that did not exist).
   - Stale build, no protoc on PATH and PROTOC unset → `ERROR: cannot build: - protoc not found: …`, EXIT=1, and the target dir was never created.
   - Fake cargo first on PATH with PROTOC=dummy: exit 101 → ps1 `cargo build --release -p konnect failed (exit 101).` (sh `…failed.`), EXIT=1, no target dir. Exit 0 → `built …`, `copied …`, EXIT=0.
   - The fake recorded `args=["build","--release","-p","konnect"] cwd=<S>\repo PROTOC=<dummy>` (sh in `C:/…` form), with ps1 CMake = the VS BuildTools CMake bin prepended. The real cargo never ran.
8. Items 6/7 + R5 (dry runs, `-ClaudeConfig <fixture>`; the sh ran with `HOME=<S>\home`). The same target in both scripts:
   - `top` → `…\t\top\konnect.exe`.
   - `shadow` → `WARNING: Claude Code gives a project's own konnect entry precedence …` / `- project C:/work/board -> <S>\t\proj\konnect.exe`, then `-> <S>\t\top\konnect.exe`. The same-path project (other case, `/`) is not listed.
   - `one` → `…\t\proj`. `two` → EXIT=1 `registers konnect at several paths:` listing both.
   - Wrappers exit 1 `not a path to a konnect executable (a wrapper or a command with arguments)`: `cmd /c C:\tools/konnect.exe`, `cmd` + args, `C:\Windows\System32\cmd.exe` + args, and a project `C:\tools\konnect.exe --stdio`.
   - `none`, `missing` (`no Claude config at …`) and `empty` → the default target. `bad` → EXIT=1 (ps1 `cannot parse …; pass -Target.`). `casedup` → `…\t\cd`. `-Target` with `two.json` → `from -Target`.
   - ps1 item 7: `capital` (`"Konnect"`) → `no konnect MCP server registered`. `posix` (`/c/Users/…/t/posix/konnect.exe`) → `NOTE: read the POSIX-style path '/c/…' as 'C:\…'` and `-> <S>\t\posix\konnect.exe`. `/usr/bin/konnect` → EXIT=1 `a path without a drive letter`.
   - Relative config: started from `<S>\repo`, `Set-Location <S>\fx; & install.ps1 -ClaudeConfig .\shadow.json -DryRun …` → `from top-level … in <S>\fx\shadow.json`, EXIT=0.
9. Item 8 (sh dry runs on `bad.json`). jq 1.7.1 → `not valid JSON (checked with jq)`, EXIT=1. A jq-1.6 shim (exit 5 → 2) gives the same. With jq removed from PATH, python → `(checked with python)`, EXIT=1; python with an empty file → default target; python reads `shadow` correctly. A PATH with no reader → `WARNING: neither jq nor python is available …; falling back to the default target.`
10. Item 9 messages.
    - ps1 `-Help`: `Run it from an open PowerShell window, …` / `not with Explorer's "Run with PowerShell": that window closes …`.
    - PROTOC (real run, before any change): ps1 `To keep PROTOC for every new window: [Environment]::SetEnvironmentVariable('PROTOC','<path to protoc.exe>','User')`; sh `…every new terminal, run in PowerShell: [Environment]::SetEnvironmentVariable(…)`.
    - Fresh install: `backup:  (none - fresh install: there was no konnect at … before)` and `undo (…) - this was a fresh install, so undoing it moves the new file aside:`. Paste 1 moves the file to `-rolledback`; paste 2 changes nothing.
    - RETRACE (dry run): `RETRACE_PYTHON only reaches programs started after it is set: close Claude Code completely` / `(every window, and any terminal running it) and start it again from a new terminal or the Start menu.`
11. R6/R7.
    - Identical binary → `identical binary already at … - skipping backup and copy`, `rollback: nothing to roll back.`, fingerprint `9732cdc31309` unchanged.
    - With 26 upcoming `.bak` stamps pre-created → `renamed … -> …konnect-0.11.0-old-20260922-104817-2.exe.bak` (sh `…-104823-2.exe.bak`); `.bak` = old sha, `konnect.exe` = new sha.
12. R9/R10/R11: full dry run with `-Client both`, a stale build, fake-old at the target and `-RetracePython <main .venv-retrace>` (no `-SkipInit`, allowed because `-DryRun` was set).
    - Plan printed: `would run: cargo build`, `would rename`, `would copy`, `init` / `status --client claude` / `init --client codex` / `status --client codex`, `retrace 0.3.0 importable`, then ps1 `would set RETRACE_PYTHON (user): (unset) -> …` or sh `would run: setx RETRACE_PYTHON "…"`, then `DRY RUN - nothing was changed.`
    - Target fingerprint `5b7e1d7ad860` and repo fingerprint `82fb421e6c7f` (`.git` included) were unchanged in both. The fakes received only `1 --version`.
    - `C:\Python314\python.exe` → `cannot import retrace` plus a NOTE. A missing path → `does not exist`.
13. R9 live success path (Done-count match, status call): not re-run, because the rule forbids init outside the one failure exception. `git diff -U0 897c4a0..334a8ac` has no changed line containing `Done:`, the skill/agent counts, `status --client` or the init call. Only the `$initClients`/`init_clients` bookkeeping was added. That code is byte-for-byte what passed live with a fake in round 1 (02-qa Evidence 8).

## For the next agent

1. Orchestrator: both scripts are ready to merge from the qa side. `orc merge` also needs the reviewer's re-verify DONE.
2. For the user's first real run, the reviewer's note still applies: it renames the running 0.12.0 exe, runs init against the real `~/.claude`, and sets User RETRACE_PYTHON. Tell them to close Claude Code and open a new terminal afterwards. PROTOC is still not in the user environment, but no build is needed while the main checkout's build is current.
3. Harness gotchas that nearly produced false results this round (all caught and re-run):
   - This tool collapses `\\` to `\` in commands and heredocs. Build Windows paths with `cygpath -w`, and write generators with the Write tool.
   - Git Bash rewrites POSIX-looking env values and args (`/c/…` → `C:/…`) for native python/jq. Write POSIX fixtures with bash `printf`.
   - `powershell -File` ignores a file without a `.ps1` extension, so a "paste" silently does nothing. Assert that paste 1 changed state before crediting paste 2 for not changing it.

## Deferred findings

1. Low (ps1): `Test-KonnectExecutablePath` accepts a drive-relative command. A malformed `"C:cmd /c C:\tools\konnect.exe"` passed: `IsPathRooted` is true for `C:…`, and the leaf is `konnect.exe`. `GetFullPath` throws, so the raw string came back and the dry run printed `-> C:cmd /c C:\tools\konnect.exe`, EXIT=0. The sh refuses it. This is artificial input, and a real run would most likely fail at the copy (not run). Fix: require `^[A-Za-z]:[\\/]` or `^\\\\` before accepting.
2. Low (sh, Git Bash only): a driveless `/usr/bin/konnect` command is read as Git's own `C:\Program Files\Git\usr\bin\konnect` (dry run `->` that path, EXIT=0), while the ps1 now refuses it (item 8 was ps1-only by brief). Claude Code (node) would resolve it differently.
3. Cosmetic: when the "target still exists after the rename" guard fires, the parked name carries the new version tag although the file parked is whatever sat at the target. Harmless.
4. Carried over, not re-scoped: `.gitattributes *.sh eol=lf`; schematic-viewer.exe staleness; `-Client codex` semantics; python `-B`; the Codex `Done:` count is unchecked; `.bak` files are never pruned.
