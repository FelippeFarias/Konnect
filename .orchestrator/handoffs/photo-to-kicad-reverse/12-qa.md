---
change: photo-to-kicad-reverse
task: "VERIFY round 2"
agent: qa
verdict: DONE
failing_layer: none
---

## Result

**47 PASS / 0 FAIL** at HEAD `39d4b4d` (clean worktree) across 31 task criteria
and 16 spec scenarios. All nine round-1 FAIL rows re-run and now PASS; no
previously passing row regressed. No new test files were needed — every
criterion and every revised scenario now has a runnable check or a citable
artifact line.

1. **The blocker is really fixed at the boundary.** `review_map_schema()`
   declares the map's nested `properties` plus `"additionalProperties": true`
   on the map, each component and each net. Driving `save_photo_review_map`
   through `ToolRouter::load` → `def.input_validator.validate` → `def.handler`
   with a map carrying an unknown top-level key (`reviewer_note`), an unknown
   component key (`why_unknown`) and an unknown net key (`measured_with`) is
   accepted, and `load_photo_review_map` returns all three — while `""` is
   still normalized to `null` (E5). The whole photo-intake e2e binary now
   routes every call through the compiled validator; the round-1
   `call_bypassing_schema` escape hatch is gone.
2. **The regression is now permanently guarded, not probed.** My round-1
   throwaway (E17) is superseded by `fixed_records_are_closed_and_only_reviewed_maps_are_extensible`
   (`router/mod.rs:185`), which walks every registered tool's compiled schema,
   demands an explicit `additionalProperties` policy, and allowlists exactly the
   four photo-intake paths with the reason. Judge that reason, not the green:
   it is the only thing between "hand annotations survive" and "any garbage is
   accepted into `map`".
3. **The three doc/spec-layer FAILs closed by revision, not by padding.** Task
   6.2's threshold is now "at least 2" and `grep -c photo_intake tool-directory.md`
   = 2 (G13); the four handoff/gate scenarios now name
   `kicad-schematic-build-agent.md`'s "Building from an approved photo-intake
   map" section, which exists at `:36` and does instruct refusal on a false
   `approval_valid` (quoted in Evidence 4).
4. **The three must-check behaviours beyond the tables hold.**
   `used_fallback` with `detection:true, ocr:false` → `true`
   (`photo_intake.rs:529`, test at `:2164`); the scan response carries
   `python_path` and `candidates_tried` spelled as `check_retrace` spells them,
   asserted against a real interpreter in the live test; `subprocess_arg` maps
   `\\?\UNC\srv\share\…` back to `\\srv\share\…` and its test passes.
   `retrace_extras_expected` is gone from every source and doc (only stale
   `target/` binaries match).
5. **All four gates green**, plus both live retrace runs, and the real user's
   home still has no `~/.local/share/retrace` afterwards.

### Task criteria (31)

| # | Acceptance (abridged) | Result | Evidence |
|---|---|---|---|
| 1.1 | `default_user_config()` returns `photo_intake` with **both** fields; `config::` passes | PASS | G1 (`config.rs:59-62`, exactly `retrace_python_path` + `retrace_timeout_seconds`), E2 (`config::` 13 passed, incl. `default_photo_intake_config_is_exactly_the_keys_something_reads`) |
| 1.2 | `photo_intake.rs` exists, declared in `tools/mod.rs`, builds | PASS | G2 (`tools/mod.rs:23`), E1 |
| 1.3 | Unit test: a `run_retrace` subprocess never writes under the real `HOME`/`USERPROFILE` | PASS | E3 `runner_tests::run_retrace_redirects_home_and_userprofile_away_from_the_real_home` |
| 1.4 | Unit tests cover the 3 probe scenarios; `photo_intake::` passes | PASS | E3 `check_retrace_reports_a_full_install_with_both_extras`, `…_absence_as_a_fact_not_an_error`, `…_a_base_install_without_ml_extras` |
| 1.5 | `pub(crate) async fn effective_config`; project overrides user for a `photo_intake.*` key; `config::` passes | PASS | G3 (`config.rs:163`), E2 (incl. `layering_puts_built_in_defaults_under_a_config_file_that_predates_a_key`) |
| 2.1 | `handle_scan_pcb_photo` defined; schema requires `image_path`+`project_dir`, offers only `python_path`/`timeout_seconds`, no `output_dir` | PASS | G4 (`photo_intake.rs:1099-1122` — exactly those 4 properties), E3 `scan_schema_requires_image_and_project_and_offers_no_output_dir` |
| 2.2 | Fixture round-trips through `RetraceAnalysis`; `""` → `None` | PASS | E3 `captured_analysis_round_trips_through_the_typed_structs`, `absent_strings_deserialize_to_none_not_some_empty` |
| 2.3 | Fixture byte-identical to the Windows capture; `photo_intake::` passes with no Python | PASS | E8 (`cmp` clean, sha256 `84b7dfb1…`); E3 run has no `RETRACE_PYTHON` set and the only Python-needing test is `#[ignore]`d |
| 2.4 | `--ignored` live test asserts `used_fallback` **and** the `fallback_evidence` sub-fields | PASS | E6; assertions at `photo_intake.rs:3118-3145` (all four raw signals + the D11 stderr-literal pin) |
| 2.5 | `image.workspace = true` in `[dev-dependencies]`; helper writes a readable PNG | PASS | G5 (`Cargo.toml:47`), E3 `synthetic_board::helper_writes_a_readable_png` |
| 3.1 | Structs build, `Serialize`/`Deserialize`, `subcircuit_hints` optional/absent-tolerant | PASS | E1, E3 `a_review_map_round_trips_and_tolerates_absent_subcircuit_hints` |
| 3.2 | Tests for "persists as editable JSON" + "low-confidence flagged" + client `approved: true` overwritten | PASS | E3 `a_freshly_scanned_map_persists_as_editable_json`, `low_confidence_components_are_flagged_never_auto_corrected`, `save_alone_never_approves`; E5 (`map["approved"] == false` after a save) |
| 3.3 | Test for "survives across sessions"; `map_id` with `..`, `/`, `\` rejected touching no path | PASS | E3 `the_review_map_survives_across_sessions_with_hand_edits_intact`, `map_id_traversal_is_rejected_and_reads_nothing` |
| 3.4 | Tests for "approval requires an explicit call" and "editing an approved map revokes approval" | PASS | E3 `approve_records_the_hash_and_a_timestamp`, `editing_an_approved_map_revokes_approval_and_undo_does_not_restore_it` |
| 3.5 | A test named for "save alone never approves" passes | PASS | E3 `review_map_tests::save_alone_never_approves` |
| 3.6 | Pinned digest; `saved_at`/`subcircuit_hints` outside it; any `components`/`nets` change moves it | PASS | E3 `the_content_hash_of_the_fixture_map_is_pinned`, `saved_at_and_subcircuit_hints_are_outside_the_content_hash`, `editing_any_reviewed_field_changes_the_content_hash`, `every_schema_key_is_either_hashed_or_deliberately_not` |
| 4.1 | `ALL_TOOLSETS` entry + `build_tools_for` arm; `registry_tool_counts_match_reality` passes | PASS | G6 (`registry.rs:120`, `:197`), E4 |
| 4.2 | `cargo build -p konnect-core` succeeds (a bad schema panics at catalogue init) | PASS | E1; and now stronger — E1's `fixed_records_are_closed_and_only_reviewed_maps_are_extensible` validates every tool's compiled policy, not just that it compiles |
| 4.3 | A test asserts `registry::tools_for("photo_intake")` returns the 5 named tools | PASS | E4 `router::tests::photo_intake_exposes_exactly_its_five_tools_in_order` (+ `scan_contract_tests::photo_intake_exposes_its_tools_by_name`) |
| 5.1 | `SKILL.md` exists; every `load_toolset('photo_intake')` example lists only D2 tool names | PASS | G7 (`SKILL.md:59` — the five, and only the five) |
| 5.2 | `references/review-map-schema.md` exists; named ≥ 1× in `SKILL.md` | PASS | G8 (`grep -c` = 1) |
| 5.3 | Agent file exists; frontmatter `skills:` is exactly `konnect` + `kicad-photo-intake` | PASS | G9 (`pcb-photo-intake-agent.md:5-7`) |
| 5.4 | `grep -c "pcb-photo-intake-agent" konnect/SKILL.md` ≥ 1 | PASS | G10 = 2 |
| 5.5 | `manifest.rs` shows the new `include_str!` entries | PASS | G11 (`manifest.rs:124,125,128,149,150`) |
| 5.6 | `--test asset_references` passes, all named tests | PASS | E9 — 12 passed, incl. all 7 named plus `agents_make_claimed_evidence_executable` |
| 5.7 | `backticked_tool_names_…` passes; no added `NOT_TOOLS` name is a top-level tool property | PASS | E9; E11 `not_tools_allowlist` 3 passed (my round-1 guard, still live after the block gained `candidates_tried`/`pattern_name`/`component_roles`/`is_partial` and lost `retrace_extras_expected`) |
| 6.1 | `grep -rn "photo_intake\|retrace" README.md` returns ≥ 1 added line | PASS | G12 — 6 lines (`README.md:304-310`) |
| 6.2 | `--test doc_tool_counts` exits 0 **and** `grep -c "photo_intake" tool-directory.md` ≥ **2** | **PASS** (was FAIL) | E10 (6 passed, incl. `tool_directory_section_headings_match_the_registry`, `tool_directory_lists_every_registered_tool`); G13 = **2** ≥ 2 after the planner relaxed the threshold from 6 |
| 7.1 | `cargo fmt --check` exits 0 | PASS | G16 |
| 7.2 | `cargo clippy --all-targets` exits 0, no new warnings | PASS | G17 — 0 lines matching `^(warning\|error)` |
| 7.3 | `cargo test -p konnect` and `-p konnect-core` both exit 0 | PASS | E1, E7 |

### Spec scenarios — `specs/photo-intake/spec.md` (16)

| Requirement · Scenario | Result | Evidence |
|---|---|---|
| Probe · retrace and extras fully installed | PASS | E3 `check_retrace_reports_a_full_install_with_both_extras` |
| Probe · retrace missing entirely (fact, not error) | PASS | E3 `check_retrace_reports_absence_as_a_fact_not_an_error`, `resolving_a_nonexistent_interpreter_yields_attempts_not_a_panic` |
| Probe · base present, ML extras absent | PASS | E3 `check_retrace_reports_a_base_install_without_ml_extras`; E6 confirms live (this machine has neither extra) |
| Scan · succeeds on a supported image | PASS | E6 — real retrace scan, components non-empty, `analysis_json_path` under the project, duration inside the timeout |
| Scan · runs without ML extras (result + flags) | PASS | E3 `used_fallback_prefers_stderr_and_falls_back_to_the_extras_probe`, `used_fallback_is_true_whenever_any_extra_did_not_run`; E6 asserts the four-signal derivation and pins the `YOLO not available` stderr literal |
| Scan · does not pollute the real user's home | PASS | E3 `run_retrace_redirects_home_and_userprofile_away_from_the_real_home`; E6 (control + negative); E12 — `ls C:/Users/felip/.local/share/retrace` still "No such file or directory" after both live runs |
| Scan · retrace unavailable → error pointing at `check_retrace` | PASS | E3 `unavailable_scan_points_at_check_retrace` |
| Review map · a freshly scanned map persists as editable JSON | **PASS** (was FAIL) | E5 — `save` now goes **through** the compiled validator (`photo_intake_gate_e2e.rs:36-48`) and is accepted; `load` returns the same structure. Also E3 `a_freshly_scanned_map_persists_as_editable_json`, E5 `save_photo_review_map_accepts_a_real_review_map` |
| Review map · low-confidence components flagged, never auto-corrected | **PASS** (was FAIL) | Gate opened. E5 asserts `components[1].confidence == 0.5` and `value == null` survive a validated save+load; E3 `low_confidence_components_are_flagged_never_auto_corrected` |
| Review map · survives across sessions, hand edits intact | **PASS** (was FAIL) | Gate opened. E3 `the_review_map_survives_across_sessions_with_hand_edits_intact`; E5 shows `reviewer_note`/`why_unknown`/`measured_with` round-tripping through the published schema |
| Gate · unapproved map cannot reach schematic build (revised) | **PASS** (was FAIL) | Both halves the revised scenario names: `kicad-schematic-build-agent.md:49-54` (quoted in Evidence 4) instructs `INCOMPLETE` and no placement when `approval_valid` is false, and E3 `load_reports_approval_valid_false_after_an_out_of_band_edit` proves the flag goes false on an out-of-band edit |
| Gate · approval requires an explicit call | PASS | E3 `save_alone_never_approves`, `approve_records_the_hash_and_a_timestamp`, `approving_a_map_that_was_never_saved_is_an_error_not_a_new_file`; E5 (approve is reachable through the validator) |
| Gate · editing an approved map revokes approval | **PASS** (was FAIL) | Gate opened — the WHEN clause (a second validated `save`) is now reachable. E3 `editing_an_approved_map_revokes_approval_and_undo_does_not_restore_it`, `approve_refuses_a_map_broken_by_a_hand_edit`; E5 (approve → re-save identical content keeps it, per `resaving_identical_content_keeps_the_approval`) |
| Handoff · approved map builds a component-complete schematic (revised) | **PASS** (was FAIL) | `kicad-schematic-build-agent.md:55-59` + `:64-66` — "Place one real library symbol per component whose `approved` is true, matched by its `type` and `value` against a real library search (`search_symbols`)" and retrace's synthetic pin numbering "never used as a pinout". Scenario states no automated test in this slice |
| Handoff · approved nets become real wiring, `run_erc` reported (revised) | **PASS** (was FAIL) | `:60-63` "Wire only the map's `nets` … with the `sch_wiring` and `sch_batch` tools" and `:67-70` "annotate, save, and collect the same direct evidence, `run_erc` included … Report the map's `saved_path` and `map_id` beside the ERC result". Prose-only by the revised scenario's own words |
| Handoff · unapproved components excluded, not guessed (revised) | **PASS** (was FAIL) | `:58-59` "A component left unapproved is excluded and named in your report — never placed on a guess at what it might be." Prose-only by the revised scenario's own words |

## Evidence

All commands in `C:\Users\felip\.orc\worktrees\konnect-3b7e2022\photo-to-kicad-reverse`,
branch `orc/photo-to-kicad-reverse`, HEAD `39d4b4d`, `git status --porcelain`
empty. `PROTOC=C:/Users/felip/tools/protoc/bin/protoc.exe`, VS 2022 BuildTools
CMake on `PATH`. I wrote no files in the worktree and committed nothing this
round.

1. **Gates.** **E1** `cargo test -p konnect-core` → exit 0 (lib 1377 tests, all
   result lines `ok`; `4 passed`, `12 passed`, `3 passed; 1 ignored` for
   `photo_intake_gate_e2e`, `0 passed; 1 ignored` doc-tests). **E7**
   `cargo test -p konnect` → exit 0, 15 result lines all `ok`, `0 failed`.
   **G16** `cargo fmt --check` → exit 0. **G17** `cargo clippy --all-targets`
   → `grep -cE "^(warning|error)"` = **0**.
2. **Targeted.** **E2** `cargo test -p konnect-core config::` → `13 passed;
   0 failed`. **E3** `cargo test -p konnect-core photo_intake::` → `48 passed;
   0 failed; 1 ignored` (every test name cited above listed as `ok`). **E4**
   `cargo test -p konnect-core registry_tool_counts_match_reality` → ok;
   `photo_intake_exposes` → `2 passed`. **E9**
   `cargo test -p konnect --test asset_references` → `12 passed; 0 failed`.
   **E10** `--test doc_tool_counts` → `6 passed; 0 failed`. **E11**
   `--test not_tools_allowlist` → `3 passed; 0 failed`.
3. **Must-check (a), the round-1 blocker, through the published contract.**
   **E5** `cargo test -p konnect-core --test photo_intake_gate_e2e` →
   `3 passed; 0 failed; 1 ignored`:
   `save_photo_review_map_accepts_a_real_review_map` (validator-only, previously
   `#[ignore]`d and red), `out_of_schema_keys_survive_a_save_and_load_round_trip`
   (unknown top-level `reviewer_note`, unknown component `why_unknown`, unknown
   net `measured_with` → all three returned by `load`, while
   `components[1].value` `""` → `null`), and
   `the_approval_gate_survives_a_full_save_approve_edit_cycle`. The harness's
   `call()` (`:36-48`) panics if `def.input_validator.validate` rejects, and the
   round-1 `call_bypassing_schema` branch no longer exists in the file.
4. **Must-checks (b)–(f).** (b) `photo_intake.rs:529`
   `let used_fallback = yolo_warning_seen || ocr_warning_seen || !extras.detection || !extras.ocr;`
   with `used_fallback_is_true_whenever_any_extra_did_not_run` (`:2164`)
   asserting `detection:true, ocr:false` → `used` while `extras_ocr`/
   `ocr_warning_seen` stay distinguishable. (c) `build_scan_response`
   (`:565-566`) emits `"python_path"` and `"candidates_tried"`; **E6**
   `RETRACE_PYTHON=… cargo test -p konnect-core photo_intake -- --ignored` →
   `scan_pcb_photo_runs_a_real_retrace_scan ... ok` (1 passed, 5.63 s), whose
   assertions (`:3100-3114`) require the reported `python_path` to be a real
   file with the same file name as `RETRACE_PYTHON` and `candidates_tried` to be
   an array; `--test photo_intake_gate_e2e -- --ignored` →
   `a_real_scan_feeds_the_gate_and_leaves_the_home_directory_alone ... ok`
   (1 passed, 1.17 s). (d) `subprocess_arg` (`:277-294`) returns
   `\\srv\share\proj\top.png` for `\\?\UNC\srv\share\proj\top.png` and leaves
   `\\?\Volume{…}` alone; `runner_tests::subprocess_arg_drops_the_windows_verbatim_prefix`
   ok in E3. (e) `grep -rn retrace_extras_expected crates/ docs/ README.md` →
   exit 1, no match (a whole-worktree grep matches only three stale
   `target/debug` binaries). (f) `kicad-schematic-build-agent.md:36`
   `### Building from an approved photo-intake map`, `:49-54`
   "Proceed **only when the response's `approval_valid` is true**. … When
   `approval_valid` is false, stop and report `INCOMPLETE`: the map needs
   `approve_photo_review_map` from the human who owns it, not a rebuild by you."
   The same gate is stated on the producing side at `SKILL.md:143,163,185` and
   `pcb-photo-intake-agent.md:101,110`.
5. **Greps.** G1 `config.rs:59-62` · G2 `tools/mod.rs:23` · G3 `config.rs:163` ·
   G4 `photo_intake.rs:1099-1122` · G5 `konnect-core/Cargo.toml:47` · G6
   `registry.rs:120`+`:197` · G7 `SKILL.md:59` · G8 = 1 · G9
   `pcb-photo-intake-agent.md:2-7` · G10 = 2 · G11 `manifest.rs:124,125,128,149,150` ·
   G12 `README.md:304-310` · G13 `grep -c photo_intake tool-directory.md` = **2** ·
   **E8** `cmp` clean + sha256 `84b7dfb1681b88d39d81c961bfe30f04af84aa930f48bb6a31b1e71ac860fbac`
   between the test fixture and `openspec/…/fixtures/analysis-windows-smoke.json` ·
   **E12** `ls C:/Users/felip/.local/share/retrace` → "No such file or directory"
   after both live runs.

## For the next agent

1. **Ship it.** Every task criterion and every spec scenario passes at
   `39d4b4d` with all four gates green; nothing in this change is left red,
   skipped, or unverified except the two `#[ignore]`d live tests, which both run
   green when `RETRACE_PYTHON` is set.
2. **At archive time, amend design D11 (~line 189).** It still writes
   `used_fallback` as `yolo_warning_seen || !extras.detection`; the code, the
   three assets and the tests now agree on the strictly more conservative
   four-signal form. The design text is the stale artifact, not the code.
3. **The new build-agent section has no executable guard** (Deferred 1). The
   four revised spec scenarios all cite its *text*; nothing fails if the section
   is deleted. One entry in `agents_make_claimed_evidence_executable`'s marker
   list (`asset_references.rs:175`) would make it permanent.
4. **Do not re-verify what is already proved.** Rounds 1-2 mutation- or
   control-checked the scoped `HOME`, the content-hash pin, `approval_valid`,
   `empty_string_as_none`, `validate_map_id`, and `overlay_known_fields`; the
   schema fix is now covered by a repo-wide compiled-schema guard
   (`router/mod.rs:185`), not a throwaway probe.
5. **The deferred findings below are all pre-existing or out-of-slice.** None
   blocks this change; all five are cheap and worth a follow-up ticket.

## Deferred findings

1. `kicad-schematic-build-agent.md`'s "Building from an approved photo-intake
   map" section is cited by four spec scenarios but guarded by no test; adding
   `approval_valid` to that agent's case in `agents_make_claimed_evidence_executable`
   (`crates/konnect/tests/asset_references.rs:175`) would fix it for one array
   entry. (Developer Deferred 1, unchanged.)
2. `NOT_TOOLS` in `asset_references.rs` still carries nine names that are real
   top-level tool parameters (`project_dir`, `lib_id`, `pin_x`, `pin_y`,
   `footprint_path`, `new_number`, `match_all`, `replace_existing`,
   `sheet_instance_path`), so the phantom-tool guard is a no-op for each.
   Pre-existing; my `not_tools_allowlist` guard is scoped to the photo-intake
   block so it stays live.
3. `portable_uri` (`library.rs:1818-1821`) has the same UNC defect
   `subprocess_arg` just lost. `subprocess_arg:279-293` is now a working
   two-branch precedent to copy. Pre-existing.
4. `read_config` (`config.rs:111`) still substitutes defaults when a config file
   exists but does not parse, so a typo in `photo_intake.*` reads as "no
   preferences set" rather than an error. Now on this feature's path too.
5. `check_retrace`'s `extras: {detection: true, ocr: true}` branch is still
   unexercised against a real install (no machine here has `ultralytics` or
   `easyocr`), and a `save` whose content hashes identically to what was
   approved still re-writes `approved: true` (`resaving_identical_content_keeps_the_approval`,
   D5 as written). Both carried forward unchanged from round 1.
