---
change: photo-to-kicad-reverse
task: "VERIFY round 3 focused regression"
agent: qa
verdict: DONE
failing_layer: none
---

## Result

**13 PASS / 0 FAIL** at HEAD `3d77ed5` (clean worktree): 7 must-check rows
(a)-(f) and 6 re-verified spec scenarios. **No regression** of the round-2
47/47 table — every criterion the four fix commits touched was re-run and is
green, and the two full-suite gates cover the rest. I wrote no files in the
worktree and committed nothing: both must-checks that could have lacked a
runnable check, (b) and (c), already have one, added by the developer.

1. **(b) and (c) are covered end-to-end through the published contract**, not
   by a handler call. `photo_intake_gate_e2e.rs`'s `call()` (`:38-49`) still
   panics if `def.input_validator.validate` rejects, and the file grew from 3
   to 5 non-ignored tests. `annotations_added_after_approval_keep_the_approval_valid`
   is exactly must-check (b) (approve → annotate at top level, in a component,
   in a net *and* in `scale_reference` → `approval_valid` still true), and
   `the_approval_gate_survives_a_full_save_approve_edit_cycle` closes it with
   the (b) tail step verbatim: an on-disk `components[1].value = "100nF"` →
   `approval_valid: false`.
2. **(c) is stronger than the must-check asked for.** `a_payload_cannot_forge_its_own_approval`
   steals a *genuine* digest from a real approval of identical content under
   another `map_id` (which is outside the hash, so the forged hash is
   arithmetically correct) and still gets `approved: false`, no `approval_valid`
   key on disk, and `load` → `approval_valid: false`.
3. **(d) is the one row that needs a word from the reader: `check_retrace` now
   returns an *error result*** (`is_error: true`, text `'project_dir' does not
   resolve`, byte-identical to `scan_pcb_photo`'s) for a non-existent
   `project_dir`. I score it PASS against D4's *intent* — D4's "always returns
   a successful `CallToolResult`" is scoped by its own em-dash clause ("missing
   engine is a reported fact, not a tool failure") and D4's third bullet already
   makes a D13 path-validation failure an error. D4's absolute "always" is now
   literally stale; that is a one-clause doc amendment (Deferred 1), not a
   behavior defect.
4. **(e) the pinned digest is untouched and passing.** The literal at
   `photo_intake.rs:2565` is still `18afc7b999d88ad5ec0320760e920b7099c257d809b5961b0e5a83d1afb0908a`
   and `the_content_hash_of_the_fixture_map_is_pinned ... ok`, even though
   `review_map_content_hash` was rewritten to project onto D8 fields — which is
   the correct outcome, since the fixture carries no out-of-schema key.
5. **The repo-wide schema guard survived the fix round unchanged.**
   `fixed_records_are_closed_and_only_reviewed_maps_are_extensible`
   (`router/mod.rs:185`) still allowlists exactly the four photo-intake paths
   with the same written reason, and that reason is now *more* true than in
   round 2: with `TOOL_OWNED_KEYS` stripped on save, an open `map` can carry a
   reviewer's annotation but can no longer carry a forged verdict.

### Must-checks

| # | Must-check | Result | Evidence |
|---|---|---|---|
| a | Four gates + both `--ignored` live tests | PASS | E1-E5 |
| b | Published schema: save → approve → load `true` → unknown key at 3 levels → load still `true` → schema-field edit → `false` | PASS | E6 `annotations_added_after_approval_keep_the_approval_valid` (`:380-467`) + `the_approval_gate_survives_a_full_save_approve_edit_cycle` (`:477-607`) |
| c | Forged `approved`/`approval_valid`/`approved_at`/`content_hash_at_approval` on a never-approved map → one `approved` (false), no `approval_valid` on disk, load `false` | PASS | E6 `a_payload_cannot_forge_its_own_approval` (`:283-366`) |
| d1 | `check_retrace` + non-existent `project_dir` → matches D4 | PASS — **error result**, not a report | E7 `check_retrace_rejects_a_project_dir_the_scan_would_reject`; D4 reading in Result 3; Deferred 1 |
| d2 | venv interpreter + real dir → `available: true` | PASS | E8 — the exact `VERSION_PROBE` under the venv exits 0 printing `0.3.0` / the venv `python.exe`, so `build_check_response`'s `Some(capability)` arm (`:484`) yields `available: true`; E5 proves the same interpreter is what `RETRACE_PYTHON` resolves to. Deferred 2 (no `--ignored` test drives the probe handler) |
| e | Pinned digest unchanged and passing | PASS | E7 + literal at `photo_intake.rs:2563-2565` |
| f | 6 spec scenarios re-verified against revised wording | PASS | table below |

### Spec scenarios re-verified (`specs/photo-intake/spec.md`)

| Requirement · Scenario | Result | Evidence |
|---|---|---|
| persisted map · a freshly scanned map persists as editable JSON | PASS | E7 `a_freshly_scanned_map_persists_as_editable_json`; E6 `save_photo_review_map_accepts_a_real_review_map`, `out_of_schema_keys_survive_a_save_and_load_round_trip` |
| persisted map · low-confidence flagged, never auto-corrected | PASS | E7 `low_confidence_components_are_flagged_never_auto_corrected`; E6 cycle test asserts `components[1].confidence == 0.5` and `value == null` survive a validated save+load |
| persisted map · survives across sessions, hand edits intact | PASS | E7 `the_review_map_survives_across_sessions_with_hand_edits_intact`; E6 `annotations_added_after_approval_keep_the_approval_valid` returns `nets[0].measured_with` and `scale_reference.measured_from` after a reload |
| gate · unapproved map cannot reach schematic build | PASS | Both halves the revised scenario names: `kicad-schematic-build-agent.md:36` `### Building from an approved photo-intake map` exists, and E7 `load_reports_approval_valid_false_after_an_out_of_band_edit`. The section is now *executable* — E4's `agents_make_claimed_evidence_executable` requires `load_toolset("photo_intake")` and the `approval_valid` marker in that file (commit `3d77ed5`), closing round 2's Deferred 1 |
| gate · approval requires an explicit call | PASS | E7 `save_alone_never_approves`, `approve_records_the_hash_and_a_timestamp`, `approving_a_map_that_was_never_saved_is_an_error_not_a_new_file`; and the revised `spec.md:53` clause ("SHALL ignore every client-supplied bookkeeping key") is now non-vacuous and proved by E6 `a_payload_cannot_forge_its_own_approval` |
| gate · editing an approved map revokes approval | PASS | E7 `editing_an_approved_map_revokes_approval_and_undo_does_not_restore_it`, `approve_refuses_a_map_broken_by_a_hand_edit`; E6 `annotations_added_after_approval_keep_the_approval_valid` proves the complement the revised wording depends on ("different component or net content" revokes; an annotation does not) |

### No regression of the round-2 47/47 table

Re-ran the criteria the four fix commits touched; all PASS, none moved.

1. `7ae849e` (hash scope) → **3.2, 3.4, 3.5, 3.6**: E7 `photo_intake::` **51 passed** (was 48; +`unknown_keys_are_outside_the_content_hash_at_every_depth`, `an_empty_string_hashes_as_the_null_save_writes_for_it`, `check_retrace_rejects_a_project_dir_the_scan_would_reject`), `every_schema_key_is_either_hashed_or_deliberately_not` and the pinned digest both ok.
2. `676c8ce` (reserved-key stripping, tool + schema descriptions) → **2.1, 4.2, 5.7**: E6 5 passed/1 ignored (was 3/1), E9 `not_tools_allowlist` 3 passed, E10 `fixed_records_are_closed_and_only_reviewed_maps_are_extensible` ok with an unchanged allowlist.
3. `12a2545` (`check_retrace` canonicalization + `tool-directory.md`) → **1.1, 1.5, 6.2**: E7 `config::` 13 passed, E4 `doc_tool_counts` 6 passed, **G1** `grep -c "photo_intake" tool-directory.md` = **2** ≥ 2.
4. `3d77ed5` (asset guard) → **5.6**: E4 `asset_references` 12 passed.
5. Everything else (1.2-1.4, 2.2-2.5, 3.1, 3.3, 4.1, 4.3, 5.1-5.5, 7.1-7.3) is untouched by the diff and covered by E1 (`1364 passed; 0 failed`) and E4 (15 binaries, `0 failed`), plus E2/E3.

## Evidence

All commands in `C:\Users\felip\.orc\worktrees\konnect-3b7e2022\photo-to-kicad-reverse`,
branch `orc/photo-to-kicad-reverse`, HEAD **`3d77ed5`**, `git status --porcelain`
empty before and after. `PROTOC=C:/Users/felip/tools/protoc/bin/protoc.exe`,
VS 2022 BuildTools CMake on `PATH`.

1. **Gates.** **E1** `cargo test -p konnect-core` → exit 0, lib `1364 passed;
   0 failed; 16 ignored`, plus `4 passed`, `12 passed`, `5 passed; 1 ignored`
   (`photo_intake_gate_e2e`), `0 passed; 1 ignored` doc-tests. **E4**
   `cargo test -p konnect` → exit 0, 15 result lines, `0 failed`
   (`asset_references` 12, `doc_tool_counts` 6, `not_tools_allowlist` 3).
   **E2** `cargo fmt --check` → exit 0, empty output. **E3**
   `cargo clippy --all-targets` → exit 0, `grep -cE "^(warning|error)"` = **0**.
   **E5** `RETRACE_PYTHON=…/.venv-retrace/Scripts/python.exe cargo test -p
   konnect-core photo_intake -- --ignored` → `1 passed` (2.19 s) and
   `--test photo_intake_gate_e2e -- --ignored` → `1 passed` (1.22 s), both exit 0.
2. **E6** `cargo test -p konnect-core --test photo_intake_gate_e2e` →
   `5 passed; 0 failed; 1 ignored`, every name listed `ok`:
   `save_photo_review_map_accepts_a_real_review_map`,
   `out_of_schema_keys_survive_a_save_and_load_round_trip`,
   `a_payload_cannot_forge_its_own_approval`,
   `annotations_added_after_approval_keep_the_approval_valid`,
   `the_approval_gate_survives_a_full_save_approve_edit_cycle`. The harness
   validates first (`:44-46`: `if let Err(error) = def.input_validator.validate(&args)
   { panic!("{name} rejected its own arguments at the schema: {error}") }`) and
   the round-1 `call_bypassing_schema` branch is still absent.
3. **E7** `cargo test -p konnect-core photo_intake::` → `51 passed; 0 failed;
   1 ignored`; **`cargo test -p konnect-core config::`** → `13 passed`.
   `check_retrace_rejects_a_project_dir_the_scan_would_reject`
   (`photo_intake.rs:2370-2403`) asserts `checked.is_error`, `scanned.is_error`,
   `response_text(&checked) == response_text(&scanned)`, and that the text
   contains `'project_dir' does not resolve`.
4. **E8** `.venv-retrace/Scripts/python.exe -c "import retrace, sys;
   print(retrace.__version__); print(sys.executable)"` (the literal
   `VERSION_PROBE`, `photo_intake.rs:54-55`) → exit 0, `0.3.0`, then
   `C:\…\.venv-retrace\Scripts\python.exe`. A successful probe is the only
   input to `capability = Some(...)`, whose arm (`:484`) emits
   `"available": true`.
5. **E9/E10** `cargo test -p konnect-core --lib -- registry_tool_counts_match_reality
   fixed_records_are_closed photo_intake_exposes` → `4 passed; 0 failed`;
   `router/mod.rs:185-215` allowlists the same four `save_photo_review_map`
   paths with the same reviewer-annotation reason. **G1**
   `grep -c "photo_intake" tool-directory.md` = 2; `grep -n "Building from an
   approved photo-intake map" crates/konnect/assets/agents/kicad-schematic-build-agent.md`
   = `36`.

## For the next agent

1. **Ship it.** Verdict DONE at `3d77ed5`. All four gates, both live tests, the
   47 round-2 rows and the 13 round-3 rows are green; nothing is red, skipped or
   unverified except the two `#[ignore]`d live tests, which pass with
   `RETRACE_PYTHON` set.
2. **Round 2's "For the next agent" items 2 and 3 are both closed.** The
   planner amended D11 to the four-signal `used_fallback` form, and `3d77ed5`
   made `kicad-schematic-build-agent.md`'s new section executable via
   `agents_make_claimed_evidence_executable`. Only the D4 wording below is open.
3. **At archive time, amend D4's first bullet** (`design.md:89`): "always
   returns a *successful* `CallToolResult`" is no longer literally true — an
   unresolvable `project_dir` is now an error result, deliberately, so the probe
   and the scan cannot disagree about which project they read. One clause
   ("…except when an argument fails D13 path validation") makes the doc true.
   Same sentence family: `check_retrace`'s own description still says "Absence
   is reported as a field, never as an error", which a reader can mis-read as
   covering the path (developer note 3).
4. **Do not re-verify what is now proved three ways.** The hash scope has a
   pinned digest, a per-depth unknown-key test and a mutation check; the
   reserved-key strip has a stolen-but-arithmetically-valid digest test; the
   open-schema decision has a repo-wide compiled-schema guard. Re-running these
   buys nothing.
5. **The reviewer's three remaining doc-layer MINORs are now two.**
   `spec.md:53` was rewritten and is no longer vacuous (proved by
   `a_payload_cannot_forge_its_own_approval`); `design.md:81` and `:159` were
   rewritten by the planner and read true at HEAD.

## Deferred findings

1. **D4's "always returns a successful `CallToolResult`" is stale by one word**
   (see For-the-next-agent 3). Doc-only; behavior is the desired one.
2. **No `--ignored` test drives `handle_check_retrace` against a real
   interpreter.** (d2) is proved by running the handler's own `VERSION_PROBE`
   directly plus the live scan's interpreter assertion, not by the probe tool
   itself. One `#[ignore]`d test calling `handle_check_retrace` with
   `RETRACE_PYTHON` and asserting `available: true` would close it.
3. **`every_schema_key_is_either_hashed_or_deliberately_not` guards only the top
   level.** A field added inside a component or a net is silently unhashed until
   someone adds it to `COMPONENT_CONTENT_KEYS`/`NET_CONTENT_KEYS`; no test
   forces that decision one level down. (Developer note 2, confirmed.)
4. **Carried forward unchanged, all pre-existing:** `portable_uri`
   (`library.rs:1818-1821`) still has the UNC defect `subprocess_arg` lost;
   `read_config` (`config.rs:111`) still substitutes defaults for a config file
   that exists but does not parse; `NOT_TOOLS` in `asset_references.rs` still
   carries nine real top-level tool parameters.
5. **`extras: {detection: true, ocr: true}` is still unexercised against a real
   install** (no machine here has `ultralytics` or `easyocr`), and a re-save
   whose content hashes identically to what was approved still re-writes
   `approved: true` (`resaving_identical_content_keeps_the_approval`, D5 as
   written).
