---
change: photo-to-kicad-reverse
task: "round B tasks 5.1-5.7, 6.1-6.2, 7.1-7.3"
agent: developer
verdict: DONE
failing_layer: n/a
---

## Result

1. All twelve tasks implemented and committed in the worktree on `orc/photo-to-kicad-reverse` (`90f1bf5`). `cargo test -p konnect` exits 0 (12 asset-guard + 6 doc-count + 57 unit + 23 plugin + the protocol/CLI suites, 0 failed), `cargo test -p konnect-core` exits 0 (1355 + 4 + 12), `cargo fmt --check` exits 0, `cargo clippy --all-targets` exits 0 with no output.
2. **5.1/5.2/5.3:** `skills/kicad-photo-intake/SKILL.md` (172 lines) + `references/review-map-schema.md` + `agents/pcb-photo-intake-agent.md`, all in the `kicad-pcb` / `kicad-pcb-layout-agent` house shape. The skill states the gate the tools enforce, including the one thing prose usually gets wrong: consumers read `approval_valid` from `load_photo_review_map`, **not** the map's own `approved` flag, which stays true on a map edited after approval. The agent's `skills:` list is exactly `konnect` + `kicad-photo-intake`, its Setup reads the konnect skill's `references/reliability-contract.md`, and its only `load_toolset` is `photo_intake`.
3. **5.4:** `konnect/SKILL.md` gained the routing bullet (photo → approved map only, never a schematic/board/library mutation, hands the path to `kicad-schematic-build-agent` which re-checks approval itself), a Decision Tree row, and `photo_intake` in the Integration row of the toolset table. **5.5:** `manifest.rs` skill entry (content + one reference) and agent entry, matching the existing `include_str!` pattern.
4. **5.6/5.7:** one commented `NOT_TOOLS` block of 28 names — the 19 from tasks.md plus `approval_valid`, `saved_path`, `candidates_tried`, `review_map`, `pattern_name`, `component_roles`, `is_partial`, and the two values `board_edge_mm` / `pull_up_resistor` that the schema example carries. No guard was weakened; `snake_words` is untouched. Every name is used by an asset — nothing was added speculatively.
5. **6.1/6.2:** README prerequisite note beside the `kicad-cli` external-dependency bullet, and the registry counts (22 toolsets, 231 registered + 7 meta = 238) plus a `photo_intake` section with one row per tool in `tool-directory.md`. **Write-set deviation:** `no_file_quotes_a_stale_catalogue_total` and `no_file_quotes_a_stale_toolset_count` sweep every `.md`/`.json` in the repo, not the four CONTRIBUTING names, so `docs/TROUBLESHOOTING.md`, `packaging/metadata.json` and `plugin/plugin.json` also had to move. Task 6.2's acceptance (`--test doc_tool_counts` exits 0) is unreachable without them.

## Evidence

1. `cargo test -p konnect --test asset_references --test doc_tool_counts` → `12 passed; 0 failed` and `6 passed; 0 failed`, including `backticked_tool_names_in_prose_exist_in_the_registry`, `call_examples_name_real_parameters`, `tools_listed_beside_a_toolset_belong_to_it`, `every_reference_is_reachable_from_its_parent_skill`, `agents_preload_existing_skills`, `top_level_skill_routes_every_bundled_agent`, `library_ids_in_guidance_resolve_against_installed_kicad` (not skipped — KiCad libs are installed here), and all five doc-count tests the A2 handoff listed as failing.
2. **The asset guard was proven to bite, not merely to pass.** Deleting `bbox_px`, `approval_valid` and `pull_up_resistor` from `NOT_TOOLS` failed `backticked_tool_names_in_prose_exist_in_the_registry` with 12 findings naming all three new files (`agents/pcb-photo-intake-agent.md:66,93,102,146`, `skills/kicad-photo-intake/SKILL.md:92,130,150,172`, `.../references/review-map-schema.md:25,34,72,118`). Restored from the pre-mutation copy; `git diff --stat` on the file shows `37 insertions(+)`, the NOT_TOOLS block alone.
3. **Task 5.7's second acceptance clause checked mechanically, not by eye.** A throwaway test built the same `parameters` set the guard builds (top-level `properties` of every registered tool) and intersected it with the 28 added names → `CLASHES: []`. Test removed afterwards (`grep -c temp_dump_params` → 0). `python_path`, `image_path`, `project_dir`, `map`, `map_id`, `timeout_seconds` are deliberately absent from the block and are named as such in its comment.
4. `cargo test -p konnect` → exit 0; `cargo test -p konnect-core` → exit 0 (`1355 passed; 0 failed; 16 ignored`); `cargo fmt --check` → exit 0; `cargo clippy --all-targets` → exit 0, no diagnostics.
5. `git diff --name-only d08ed12 HEAD` → 12 files: the 3 new assets, `konnect/SKILL.md`, `manifest.rs`, `asset_references.rs`, `README.md`, `DEV.md`, `tool-directory.md`, and the three sweep-forced files in Result 5. Working tree clean; nothing pushed.

## For the next agent

1. **Spot-check the gate prose against tool behaviour.** The skill's "The approval gate" table and Rules 9 are the claims to verify against `photo_intake.rs`: only `approve_photo_review_map` writes `approved: true`; a save re-grants it only on a byte-identical content hash; `saved_at` and `subcircuit_hints` are outside the hash; `load_photo_review_map` computes `approval_valid` server-side. All four are tested in `konnect-core` (`save_alone_never_approves`, `resaving_identical_content_keeps_the_approval`, `saved_at_and_subcircuit_hints_are_outside_the_content_hash`, `load_reports_approval_valid_false_after_an_out_of_band_edit`).
2. **NOT_TOOLS additions vs schemas.** Re-run the intersection in Evidence 3 if you distrust it — the failure mode task 5.7 exists to prevent is a name that *is* a real parameter being allowlisted, which turns the guard into a no-op for that name. The block's comment names the six excluded parameters explicitly so a future editor does not "helpfully" add them.
3. **Doc counts.** 231 = 226 + 5 and 22 = 21 + 1. The two sweep tests are repo-wide over `.md`/`.json` (skipping `target`, `node_modules`, `.git`, `.claude`, `dist`, `build`), so any *new* document quoting a catalogue total must quote these. The worktree has no `openspec/` directory, so `design.md`/`tasks.md` are outside the sweep there but are inside it in the main checkout — check them before archiving the change.
4. **Not modified, by design (D6.4):** `kicad-schematic-build-agent.md`. `agents_make_claimed_evidence_executable` and `skills_define_the_same_evidence_boundary_as_their_agents` are hard-coded case lists that do not enrol the new agent or skill, and `install.rs`'s reliability-contract assertion names only the schematic-build and design-review agents — the new agent reads the contract anyway, but no test forces it.
5. **Slice 2 handoff is prose, not code.** The skill and agent instruct `kicad-schematic-build-agent` to call `load_photo_review_map` and check `approval_valid`; nothing in the schematic toolsets enforces it yet. The spec scenario "unapproved map cannot reach schematic build" is satisfied at the map layer (a consumer cannot get a valid approval for edited content) but not at the `sch_*` tool layer.

## Deferred findings

1. `docs/TROUBLESHOOTING.md`, `packaging/metadata.json` and `plugin/plugin.json` quote catalogue totals and are guarded by a repo-wide sweep, but CONTRIBUTING still names only four files as the ones that "move together". The guidance is narrower than the guard.
2. The skill names the extras as `detection` and `ocr` in prose but never names the `fallback_evidence` sub-fields (`yolo_warning_seen`, `ocr_warning_seen`, `extras_detection`, `extras_ocr`). That was deliberate — four more `NOT_TOOLS` entries for diagnostic detail an agent can read from the response — but a reader debugging a fallback has to open the JSON to learn the field names.
3. `library_ids_in_guidance_resolve_against_installed_kicad` splits the whole file on backticks and step-by-2s the result, so a fenced code block shifts inline-span parity for everything after it. The new assets pass, but the check's notion of "inline code" is approximate in any file containing fences — which is all of them.
