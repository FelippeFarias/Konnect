---
change: konnect-orchestrator
task: tasks 4.1-4.6, 5.1-5.3 — six new bundled agents; flow steps in the schematic, layout and review agents
agent: developer
verdict: DONE
failing_layer: null
---

## Result

1. Nine commits on `orc/konnect-orchestrator` (worktree `C:\Users\felip\.orc\worktrees\konnect-3b7e2022\konnect-orchestrator`, on top of `d0eb0de`), not pushed: `59511ed` 4.1 requirements, `324b729` 4.2 architecture, `6f7af71` 4.3 sourcing, `c578a28` 4.4 library, `a674a93` 4.5 manufacture, `a9e8cba` 4.6 curator, `e8ca2ec` 5.1 schematic build, `0eae72e` 5.2 layout, `4f18dc8` 5.3 design review.
2. Each new agent copies the existing frontmatter shape (`name`, `description` with `Triggers:` and then `Anti-triggers:`, `model: sonnet`, `skills:`/`tools:` block lists, `maxTurns` 60/150/150/100/150/60). Each one carries in its own prompt: the no-`job_id` rule, every `flow_*` call shape it uses, the phase check (`flow_status` → `phase` must be its phase, otherwise `BLOCKED`), lesson triage and the five-line entry, and the handoff header and sections. No rule it needs lives only in a `references/` file.
3. Record sections defined in the agent files, since no preloaded SKILL body held them. `manufacturing.md` (in manufacture): Order contract, Design evidence, BOM integrity, Package, Indicative cost, Release notes, Checks at the purchase gate, Verdict. `schematic-evidence.md` (5.1): Validation Results, Unresolved Concerns, Layout Handoff. `placement.md` / `routing.md` (5.2): Constraint record, Placement, Images, Placement gate / Return-path…, Routing, Evidence, Routing gate. The ledgers (5.3): Findings with an action column (`FIX_BEFORE_FAB`, `ORDER_NOTE`, `FIRMWARE_REQ`, `DOC_ONLY`, `NONE`), Checked and correct, Open questions, Verdict.
4. Existing agents: additions only. The frontmatter is byte-identical, 0 removed lines. The layout hard stop is kept word for word, and in a job it becomes unconditional (new Phase 3 bullet + Hard rule 11: never routes in the run that recorded `placement.md`, never touches the board after entering `gate:placement`).
5. Decisions the design left open. (a) The order is `flow_advance` → `flow_log` handoff → return, so `DONE` means the advance was accepted. The curator closes first, then persists (the journal stays open on a closed job). (b) Producers advance only on success: architecture `PASS`; schematic not `INCOMPLETE`; layout gate `PASS` / evidence `COMPLETE`; reviewer with no open `FIX_BEFORE_FAB` and not `INCOMPLETE`; manufacture `READY`. Otherwise they return FIX/BLOCKED, the session rewinds from the review phase, and `fix_rounds` counts it. (c) See Deferred 1–2 for the tool-access mappings.

## Evidence

1. Acceptance script (`scratchpad/check_agents.py`, which reads block lists by `yaml_list`'s own rule) → all six `OK`: key order, name, `Triggers:`, model, skills, tools, maxTurns, plus each task's required strings (`load_toolset("flow")`, `flow_advance`, `flow_log`, `calls no \`flow_*\``, `BLOCKED means return BLOCKED, not advance`, `never calls \`flow_advance\``, `Physical pin-map acceptance contract`, `never edits a schematic sheet or the board`, `firmware-contract`, `INCOMPLETE`, `indicative heuristic`, `any phase before \`learn\``, `never writes a \`MEMORY.md\``).
2. 5.x greps: schematic `load_toolset("flow")` ×2 and `flow_advance` ×2, Step 9 at line 220 after Step 8 at line 204. Layout `flow_advance` ×5, load ×1. Review `flow_advance` ×5, load ×1. The frontmatter sha256 matches before and after for all three (`6dbbdd…`, `2a2421…`, `332182…`). `git diff -U0 d0eb0de..HEAD`: every hunk is `-N,0`, after the frontmatter's closing line (11/12/13), and 0 lines are removed.
3. SC command: `cargo test -p konnect --test asset_references -- agents_preload_existing_skills top_level_skill_routes_every_bundled_agent call_examples_name_real_parameters documented_toolsets_exist_in_the_registry tools_listed_beside_a_toolset_belong_to_it every_reference_is_reachable_from_its_parent_skill` → `6 passed; 0 failed`. `--test doc_tool_counts` → `6 passed`. Full `cargo test -p konnect --no-fail-fast`: every target passes except asset_references at `12 passed; 1 failed`, which is the phantom test, as at `d0eb0de`. `library_ids_in_guidance_resolve_against_installed_kicad` → ok.
4. `git diff --name-only d0eb0de..HEAD` → exactly the 9 FS agent files, in 9 commits; `git status` is clean.
5. Mutation proofs (`scratchpad/mutations.py`, restore in `finally`, each `restored byte-identical: True`). Dropping `job_id` from manufacture's `flow_advance` → `call_examples…` fails: "omits required `job_id`". An inline `skills: [konnect, kicad-curator]` → `agents_preload…` fails: "preloads no skills". Adding `export_gerber` beside sourcing's `load_toolset("flow")` → `tools_listed_beside…` fails: "lives in 'pcb_export'". Renaming the library agent → `top_level_skill_routes…` fails. requirements `maxTurns: 70` → the checker exits 1.

## For the next agent

1. **6.2: no new NOT_TOOLS name.** Phantom run: 46 → 73 flags, and the distinct names stay at the same 21 (`comm` of before/after is empty). The 27 new flags are all in agent files: `failing_layer` ×15, `prefab_review` ×7, `schematic_review` ×3, `fix_rounds` ×1, `next_step` ×1. I avoided writing `design_hash`, `stale_target`, `next_phase`, `read_name`, `preferred_distributors`. The full 21: `approved_by`, `board_revision`, `deferred_findings`, `evidence_check`, `fab_only`, `failing_layer`, `fix_rounds`, `gate_approvals`, `last_transition`, `lock_files`, `new_board`, `next_step`, `not_ok`, `pending_approval`, `pending_approvals`, `photo_to_kicad`, `prefab_review`, `queue_item`, `review_only`, `schematic_review`, `state_error`.
2. **6.3: the seven agents that call `flow_advance`**, with the toolsets they load. Requirements: `config`, `flow`. Architecture: `config`, `library`, `integration`, `flow`. Manufacture: `pcb_export`, `manufacturing`, `verification`, `sch_analysis`, `integration`, `flow`. Curator: `flow`. Schematic, layout and review add `flow`. Sourcing and library do **not** advance, yet both load `flow` (status/log/defer) and contain the string `flow_advance` in "never calls `flow_advance`", so do not enroll them as callers.
3. **6.1:** the six new stems are `kicad-requirements-agent`, `kicad-architecture-agent`, `kicad-sourcing-agent`, `kicad-library-agent`, `kicad-manufacture-agent`, `kicad-curator-agent`. All are already routed in `konnect/SKILL.md`. The new files are LF in the working tree (git converts them on the next touch); the index is LF.
4. **Gotchas.** `signature_examples` scans one line at a time, so a `flow_advance(…)` wrapped over two lines is silently unchecked. Every signature here sits on one line (`grep "flow_[a-z]*(" | grep -v ")"` is empty). The reliability-contract citation stays valid: the orchestrator corrected that; the new agents cite skills, not that file.
5. **Library agent specifics.** The pinout pages must arrive in the brief, because no tool opens a datasheet. The scratch project must sit outside the design directory: `design_state_hash` recurses, so a nested scratch `.kicad_pro/.kicad_sch/.kicad_pcb` would join the design hash.

## Deferred findings

1. `architecture-record-schema.md` §1 says Stock must come from the fabricator's current part page ("a downloaded catalogue is discovery, not stock") and the datasheet must be "validated as a real PDF". No bundled agent can do either: `mcp__konnect__*` has no fetch tool. Sourcing writes `<qty>, <catalogue date>, local catalogue` plus `located, not validated`, and cites `jlcpcb-rules.md` §2's "recheck before payment". The architecture readiness criterion "every parts-list row has a confirmed source" is therefore ambiguous. Owner: design/3.1 follow-up (orchestration.md §9 should say who confirms live stock and datasheets).
2. kicad-manufacture requires a Gerber viewer check (artifact acceptance step 5) and discharging `PREVIEW_REQUIRED` in the fabricator's portal. No Konnect tool does either, while orchestration.md §2 says "an `INCOMPLETE` package is not an exit". The manufacture agent lists both, plus live stock/price, under `## Checks at the purchase gate`, and treats `READY` as "every tool-runnable check passed". orchestration.md §4's purchase row should name that section so the session shows it. Owner: 3.1 follow-up, reviewer to confirm the reading.
3. Unverified: the library agent's disposable placement uses `place_component` on a scratch board. Whether that tool needs a live KiCad IPC session with the scratch board open was not checked.
