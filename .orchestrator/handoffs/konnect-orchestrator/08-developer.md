---
change: konnect-orchestrator
task: tasks 3.4, 6.1-6.4, 7.1-7.3 — confirmation gaps, manifest, asset guards, final gates
agent: developer
verdict: DONE
failing_layer: null
---

## Result

1. Four commits on `orc/konnect-orchestrator` (worktree `C:\Users\felip\.orc\worktrees\konnect-3b7e2022\konnect-orchestrator`, on top of `4f18dc8`), not pushed: `70bce93` 3.4, `734f2f2` 6.1, `34d9285` 6.2, `4cdb673` 6.3. 6.4 and 7.1–7.3 were green on the first run, so they have no commit. `git diff --name-only 4f18dc8..HEAD` lists 5 files, all in FS: `orchestration.md`, `architecture-record-schema.md`, `manifest.rs`, `install.rs` (tests module only; single hunk `@@ -753,0 +754,127 @@ mod tests`), `asset_references.rs`.
2. **3.4.** orchestration.md §9 gains "The session confirms live stock and datasheets". The session confirms each row inside `architecture`, before the gate, and logs one `flow_log(project_dir, job_id, kind, message)` entry per row with `kind` `evidence`. The next brief names those entries (`log` via `flow_status(project_dir, read)`). §4's purchase row names `## Checks at the purchase gate`. A new §4 bullet lists the three checks: show, run or ask, log as evidence, then `flow_gate`; a failed check is a FIX. In architecture-record-schema.md, "A confirmed row" requires the session's evidence entry. Four conditions keep the line BLOCKED: no entry, `located, not validated`, catalogue-only stock, or no part/datasheet. The PASS bullet points there.
3. **6.1.** `SKILLS` gains `kicad-architecture` (2 references) and `kicad-curator` (none), and `konnect` gains `orchestration.md`, `brief-template.md` and `handoff-template.md`. `AGENTS` gains the six stems in roster order. The manifest now ships 12 skills and 11 agents. `manifest_ships_every_asset` walks `assets/` and compares both keys and bytes with the manifest. It fails on a stray file, a duplicate, an unshipped asset, a phantom entry, or an entry embedding a different file. `reliability-contract.md` is the one declared `GENERATED_REFERENCES` entry (← `../../docs/RELIABILITY_CONTRACT.md`). The test asserts that no copy of it exists under assets.
4. **6.2, NOT_TOOLS additions** (one commented block at the array's end, each flagged by the failing run, none a tool or a top-level property). Lanes: `new_board`, `board_revision`, `review_only`, `fab_only`, `photo_to_kicad`. Phases: `schematic_review`, `prefab_review`. Defer kinds: `queue_item`, `pending_approval`. STATE/status fields: `deferred_findings`, `pending_approvals`, `gate_approvals`, `lock_files`, `fix_rounds`, `last_transition`, `next_step`, `state_error`. History/approval: `evidence_check`, `not_ok`, `approved_by`. Handoff: `failing_layer`. That is 21 names. The design names the test did not flag are left out (`design_hash`, `design_files`, `design_state_hash`, `*_at_approval`).
5. **6.3.** The schematic, layout and review tuples gain `flow` / `flow_advance`. New tuples cover requirements, architecture, manufacture and curator (`&["flow"]`, `&["flow_advance"]`). That makes seven cases. Sourcing and library are not enrolled: they load `flow` but never advance (a comment in the test says so).

## Evidence

1. 3.4: `grep -c "Checks at the purchase gate"` orchestration.md = 2. `grep -c "located, not validated"` architecture-record-schema.md = 1. `call_examples_name_real_parameters every_reference_is_reachable_from_its_parent_skill` → `2 passed`. The phantom run stayed at the same 21 names and 73 flags after 3.4, so the new prose adds no name.
2. 6.1 TDD: RED first → "these assets are never installed" listing exactly the 13 D10 files. GREEN: `install::` `16 passed`. Mutations (`scratchpad/mutate.py`, restored in `finally`, each `restored byte-identical: True`): (a) the brief-template entry embeds handoff-template → "embed a different file … brief-template.md"; (b) `GENERATED_REFERENCES = &[]` → "ships paths with no file under assets/: reliability-contract.md"; (c) the curator agent dropped from `AGENTS` → "never installed: agents/kicad-curator-agent.md".
3. 6.2: the phantom test → `ok`, and `not_tools_allowlist` → `3 passed`. Mutations: (a) dropping `new_board`, `not_ok` and `failing_layer` → 26 flags naming all three; (b) adding `"job_id"` to the new block → `no_photo_intake_allowlist_entry_is_a_real_tool_parameter` FAILED `["job_id"]`. Both restored byte-identical.
4. 6.3 mutation: removing EVERY `load_toolset("flow")` (the schematic agent has 2) from each of the seven files, one at a time → 7/7 `FAILED` with "agents/<file> does not load `flow`", 7/7 `restored byte-identical: True`.
5. Gates (exit codes in `scratchpad/g_exit.txt`): `cargo build -p konnect` 0, `cargo fmt --check` 0 (empty output), and `cargo clippy --all-targets` 0 with 0 `warning`/`error` lines. `cargo test -p konnect` 0: lib 23, main 58, asset_references 13/13, doc_tool_counts 6, not_tools_allowlist 3, protocol_stdio 18, protocol_http 2, schema_parameter_usage 5, schema_migrations 3, transaction_cli 4, release_workflow_contract 2; 0 failed; 8 ignored live/e2e. `cargo test -p konnect-core` 0: 1476 passed, 0 failed, 18 ignored, over 9 targets (lib 1442, flow_gate_e2e 3, photo_intake_gate_e2e 5, …).

## For the next agent

1. **QA first: 3.4 lives only in `references/` files.** The architecture agent still says, in its Step 6 bullet (`kicad-architecture-agent.md:122-124`), that catalogue stock is "discovery" and is re-checked "before payment". Neither that agent nor `kicad-architecture/SKILL.md:175` repeats the new "confirmed row" definition. D9 says every rule a new agent needs must sit in a preloaded body. That file is outside my FS, so I did not touch it (Deferred 1).
2. The `not_tools_allowlist.rs` check covers the new block by accident of layout. Its parser reads from the photo-intake marker to the array's `    ];`, so any block appended at the end is covered, and its failure text still says "photo-intake". Keep future blocks at the end, or widen that test.
3. `manifest_ships_every_asset` compares bytes, not only names, so a copy-pasted `include_str!` path is caught. Any future generated reference must be added to `GENERATED_REFERENCES`, and any non-`.md` file under `assets/` fails the test as stray. That is deliberate: `install_skills` would never ship it.
4. `cargo fmt` rewrote `asset_references.rs` to LF when it reflowed the 6.3 tuples, and I converted it back to CRLF before the commit. The index is LF either way, so the diff is unaffected.
5. The reviewer should confirm that the §4 purchase bullet's three checks match `kicad-manufacture-agent.md:134-140` word for word in meaning: viewer, preview while `PREVIEW_REQUIRED`, live stock and price.

## Deferred findings

1. `crates/konnect/assets/agents/kicad-architecture-agent.md` (Step 6 bullet at :122-124, readiness bullet at :141-142) and `crates/konnect/assets/skills/kicad-architecture/SKILL.md:175` still say "confirmed source" without 3.4's definition. The Step 6 bullet also implies that catalogue stock is acceptable until payment. Fix: one sentence in each preloaded body — a row is confirmed only by the session's `flow_log` evidence entry, and catalogue-only stock or `located, not validated` keeps the line BLOCKED. The architecture agent should also read `log` when the brief names it. Owner: orchestrator (a 3.4 follow-up; outside this FS).
2. `README.md:345` says "10 skills + 5 agents bundled", but the manifest now ships 12 skills and 11 agents. No guard sweeps that row (`doc_tool_counts` counts tools only). Owner: docs follow-up for this change.
3. `not_tools_allowlist.rs` scopes itself by the photo-intake marker, yet in practice it checks everything down to the array's end. Its name and messages say "photo-intake". Owner: test-hygiene backlog (rename, or parse every commented block after a chosen marker).
