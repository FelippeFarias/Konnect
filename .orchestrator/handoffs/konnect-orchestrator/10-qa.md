---
change: konnect-orchestrator
task: VERIFY — all 34 task acceptance criteria and all 52 spec scenarios (flow-toolset 33, hardware-orchestration 10, agent-roster 9)
agent: qa
verdict: DONE
---

## Result

1. **34/34 task criteria PASS, 52/52 scenarios PASS.** No FAIL. Verified at `84f7e0f` plus 5 additive QA commits on `orc/konnect-orchestrator`: `8325d11`, `4ab71ba`, `6c159c9`, `502be0b`, `62d2045`. Final HEAD is `62d2045`. Nothing pushed. No implementation, skill, agent, manifest or openspec file was changed.
2. **Full battery re-run at `62d2045`: all green.** `cargo fmt --check` exit 0. `cargo clippy --all-targets` exit 0 with 0 warnings. `cargo build -p konnect` exit 0. `cargo test -p konnect` exit 0 (asset_references 18/18, doc_tool_counts 6/6, not_tools_allowlist 3/3, bin 58/58). `cargo test -p konnect-core` exit 0 (lib 1442 passed / 16 ignored, flow_contract_e2e 11/11, flow_gate_e2e 3/3). This run replaces the orchestrator's `4cdb673` run.
3. **Gaps I found and closed with runnable checks.**
   - (a) `flow_gate_e2e` sent only 4 of the 6 tools through the router. `flow_log` and `flow_defer` had only ever been called on their handlers. All six now go through the router.
   - (b) The unit tests split "autonomous mode still stops at purchase" across two different jobs. It is now walked in one job.
   - (c) "Any flow tool refuses a non-project folder" was tested for 2 of the 6 tools. It is now tested for all 6.
   - (d) Task 1.1's round-trip objective had no `ç` (it has `ã`). A router-level round trip with `ç` now exists.
   - (e) Nothing kept the prose scenarios, or the 3.4/3.5 confirmation rule, alive. Section-scoped content guards now cover them.
4. **The three user gates cannot be skipped.** In one autonomous job, the session approves `placement` with empty words (`approved_by: session`). `purchase` with `""`, `"   "` or `"\n\t"` is refused (`field: user_words`), and no gate file is written. Leaving to `learn` is `stale_target` on `gate:purchase`. The purchase approval is accepted only with the user's words, and the job then runs to `closed`. A job whose last phase is `gate:purchase` cannot `closed` around the gate, with or without a `reason`, in either mode. A lane subset without its gate is refused by name, in both modes, and creates nothing: `[architecture, schematic]`, `[placement, routing]`, `[manufacturing]`, `[manufacturing, learn]`.
5. **SC3 met.** The new 3.4/3.5 guard `architecture_confirmation_rule_stays_guarded` passes. It went red on 4 deliberate removals, one in each of 4 files. Each file was restored byte-identical (sha256 checked) and `git status` was clean after each probe (Evidence 4).

## Evidence

1. **Task criteria.** Test names are `tools::flow::<module>::<fn>` unless a file is named. "QA" marks a test I added.

| Task | Result | Evidence |
|---|---|---|
| 1.1 | PASS | `foundation_tests::a_hostile_objective_round_trips_through_state_md` (has `---`, `"`, newline, `:`, backtick; LF and CRLF). `the_validator_names_the_entry_it_rejects`: out-of-order, repeat, unknown, gate-first and `[placement, routing]`, each naming its entry, plus the empty sequence. `readiness_accepts_only_an_exact_trimmed_pass_line` and `a_blocked_readiness_line_carries_its_reason`. `ç` was absent from the unit objective (flow.rs:3232); QA `flow_contract_e2e::a_hostile_objective_round_trips_through_the_published_tools` round-trips `ç` through flow_start and flow_status, and STATE.md has exactly 2 `---` lines. `--lib tools::flow::`: 56 passed. |
| 1.2 | PASS | `status_tests::status_without_a_flow_directory_reports_no_job_and_creates_nothing`, `status_lists_every_kicad_lock_beside_a_covered_file`, `a_broken_state_file_is_reported_not_fatal`, `a_read_name_outside_the_readable_set_is_an_invalid_argument` (`../x` → `invalid_argument`, field `read`) |
| 1.3 | PASS | `start_tests::a_second_start_while_a_job_is_active_is_a_conflict_and_changes_nothing` (bytes equal), `missing_or_invalid_arguments_are_refused_before_anything_is_created` (no `.konnect`), `a_closed_job_lets_the_next_one_start`, `a_directory_without_a_top_level_project_is_refused` |
| 1.4 | PASS | `advance_tests::leaving_requirements_with_its_record_writes_it_and_moves_the_phase`, `a_stale_record_on_disk_does_not_satisfy_leaving_architecture` (message "requires architecture.md", STATE bytes and phase unchanged), `a_skip_ahead_or_foreign_target_is_refused`, `a_record_belonging_to_another_phase_is_refused` |
| 1.5 | PASS | `rewind_tests::a_fix_round_is_a_rewind_with_a_reason` (`fix_rounds.schematic_review` = 1), `closing_from_a_middle_phase_needs_a_reason_and_ends_the_job`, `the_evidence_check_reports_what_the_ring_confirms_and_never_refuses` (confirmed `[run_erc]`, absent `[render_schematic_png]`). QA repeats all three through the router. |
| 1.6 | PASS | `gate_tests::approving_architecture_against_a_blocked_readiness_writes_no_gate_file`, `an_approval_without_the_users_words_is_refused_in_guided_mode_and_at_purchase`, `autonomous_mode_lets_the_session_approve_placement` (`approved_by: session`), `a_board_changed_since_the_gate_was_entered_is_a_stale_target` |
| 1.7 | PASS | `journal_tests::a_decision_needs_why_and_rollback_and_evidence_takes_no_scope` (journal snapshot unchanged), `each_handoff_gets_its_own_numbered_file` (01-review, then 02-review), `two_concurrent_defers_both_land` (`tokio::join!`). QA repeats them through the router. |
| 1.8 | PASS | `--lib router::`: 19 passed, including `registry_tool_counts_match_reality`, `fixed_records_are_closed_and_only_reviewed_maps_are_extensible` and `flow_exposes_exactly_its_six_tools_in_order`. The router/mod.rs diff is only the new test, so the allowlist is unchanged. The registry entry is at :156, after manufacturing at :150. |
| 1.9 | PASS | `--test flow_gate_e2e`: 3 passed. Its `call()` panics when the schema rejects a call, so an uncallable schema fails the test. It covers 4 of 6 tools; QA `flow_contract_e2e` covers all 6. |
| 1.10 | PASS | `doc_tool_counts` 6/6. `grep -c` on tool-directory.md = 6. The heading `` ### `flow` · 6 tools `` is at :499. The doc_tool_counts.rs diff is 2 names plus the comment; rustfmt re-flowed the array onto several lines and nothing else changed. |
| 2.1 | PASS | Both files exist. SKILL.md names each schema file once and holds `Readiness: PASS` and `Readiness: BLOCKED` 4 times each. The named tests pass. |
| 2.2 | PASS | The schema names design-calculations.md (1), jlcpcb-rules.md (1) and datasheet-audit.md (2). Its readiness section matches D5: last non-empty trimmed line, BLOCKED plus a reason, only blank lines after. |
| 2.3 | PASS | The file exists and there is no `references/`. All 3 destinations are named (2, 3 and 2 lines). Its only signature, `flow_log(project_dir, job_id, kind, message, role, scope)` at :57, names all four required parameters. |
| 3.1 | PASS | `evidence_check\|get_recent_calls` = 3 lines; `Readiness: PASS` = 2. The named tests pass. All 9 D9 phase agents are named (QA `orchestration_reference_keeps_its_protocol_rules`). |
| 3.2 | PASS | handoff-template names `failing_layer` with all 3 layers and "required on `FIX`". brief-template has 8 field rows and "Files Scope never includes `STATE.md`". Both are held by a QA guard. |
| 3.3 | PASS | The named tests pass. `git diff bbd5efb..HEAD -- konnect/SKILL.md \| grep -c '^-[^-]'` = 0. The 3 references are named (3, 1 and 1 lines). The Orchestrator section at :126 precedes the Decision Tree at :152. |
| 3.4 | PASS | `Checks at the purchase gate` = 2 in orchestration.md; `located, not validated` = 1 in the schema. The named tests pass. Now also guarded by QA `architecture_confirmation_rule_stays_guarded`. |
| 3.5 | PASS | `located, not validated` appears once in SKILL.md and once in the agent. The agent frontmatter sha256 is `bc32890591a5fd3b` (578 B) at both `4cdb673` and HEAD. asset_references and doc_tool_counts pass. README:345 says "12 skills + 11 agents", matching 12 skill dirs and 11 agent files. |
| 4.1–4.6 | PASS | QA `orchestrator_agents_keep_their_contract`: exact `skills` block lists, `tools` = `[mcp__konnect__*]`, `model: sonnet`, `maxTurns` 60/150/150/100/150/60, and each criterion's body markers. Examples: sourcing Hard rules 1–2; the library pin-map contract (which exists in kicad-library/SKILL.md:123); the manufacture firmware-contract non-goal, `INCOMPLETE` and `indicative heuristic`; the curator's anti-trigger, brain rule and no-`MEMORY.md` rule. |
| 5.1 | PASS | Step 9 (:220) comes after Step 8 (:204). It is job-only and advances into `schematic_review` with `schematic-evidence.md`, held by a QA guard. The frontmatter sha256 is identical at bbd5efb and HEAD (`2412cf77…`). |
| 5.2 | PASS | 5 lines contain `flow_advance`. The file loads `flow`. "**Never route in the same run**" is in Phase 3, and hard rule 11 repeats it. The frontmatter is identical (`776352d8…`). |
| 5.3 | PASS | "You still never mutate the design" is stated. Under a multi-reviewer merge it never calls `flow_advance`. Single-reviewer mode records the ledgers. The frontmatter is identical (`9e115050…`). |
| 6.1 | PASS | Build exit 0. `install::tests::manifest_ships_every_asset` passes. The test walks the filesystem (install.rs:780-807). Probe: adding `agents/kicad-probe-agent.md` makes it fail, naming that file; the file was then removed. |
| 6.2 | PASS | The backticked-names test passes. Probe A: dropping the 21 flow NOT_TOOLS names gets all 21/21 flagged. Probe B: adding `job_id` makes `not_tools_allowlist` fail with "real top-level tool parameters … ["job_id"]". |
| 6.3 | PASS | 7 cases require `flow` and `flow_advance`. Probe: changing `load_toolset("flow")` to `("flo")` in each of the 7 agents makes each fail with "does not load `flow`". All 7 were restored byte-identical. |
| 6.4 | PASS | asset_references 18/18; `install::` 16 passed |
| 7.1 | PASS | `cargo fmt --check` exit 0 at `62d2045` |
| 7.2 | PASS | `cargo clippy --all-targets` exit 0, 0 warnings, at `62d2045` |
| 7.3 | PASS | `cargo test -p konnect` and `cargo test -p konnect-core` both exit 0 at `62d2045` |

2. **Scenarios.** FT = specs/flow-toolset, HO = specs/hardware-orchestration, AR = specs/agent-roster. "e2e" = QA `crates/konnect-core/tests/flow_contract_e2e.rs`. "AR-guard" = QA tests in `crates/konnect/tests/asset_references.rs`.

| Scenario | Result | Evidence |
|---|---|---|
| FT the flow toolset is discoverable | PASS | `flow_exposes_exactly_its_six_tools_in_order`; e2e `list_toolboxes_and_load_toolset_expose_the_six_flow_tools` (tool_count 6, orchestration, loaded false→true, six in order) |
| FT calls pass the published schema | PASS | `schema_tests::the_published_schemas_accept_the_documented_calls`. flow_gate_e2e plus e2e send every documented shape of all 6 tools through the compiled validator before dispatch. |
| FT no caller path reaches the flow directory | PASS | `a_read_name_outside_the_readable_set_is_an_invalid_argument` (9 names); e2e `status_reports_the_design_hash_the_locks_and_the_records_it_is_asked_for` (4 names via router) |
| FT a folder that is not a KiCad project is refused | PASS | e2e `every_flow_tool_refuses_a_folder_that_is_not_a_kicad_project`: all 6 tools, field `project_dir`, no `.konnect` |
| FT status on a project with no active job | PASS | `status_without_a_flow_directory…`; e2e (job and phase null, `.konnect` absent) |
| FT status reports the current design hash and any lock files | PASS | `status_lists_every_kicad_lock…`; e2e (64-hex hash plus both `.lck`) |
| FT an unreadable state file is reported, not fatal | PASS | `a_broken_state_file_is_reported_not_fatal` |
| FT an agent reads its input records through the tool | PASS | `status_reports_the_job_and_reads_records_through_the_tool`; e2e (contents plus `missing: [architecture.md]`) |
| FT a job cannot start while one is already active | PASS | `a_second_start_while_a_job_is_active…`, `racing_starts_open_exactly_one_job` |
| FT an invalid phases array is rejected | PASS | `the_validator_names_the_entry_it_rejects` (all 5 kinds); `missing_or_invalid_arguments…` (no job opened); e2e `a_lane_subset_that_drops_a_gate_is_refused` |
| FT an unrecognized lane is rejected | PASS | schema_tests (`sideways`); e2e `an_unknown_lane_is_refused_by_the_published_schema` (3 values, nothing written) |
| FT a closed job lets the next one start | PASS | `a_closed_job_lets_the_next_one_start`, `closing_from_a_middle_phase…` (start after abandon) |
| FT advancing without the required record is refused | PASS | `a_stale_record_on_disk_does_not_satisfy_leaving_architecture` |
| FT advancing writes the supplied records and moves the phase | PASS | `leaving_requirements_with_its_record…` (record content, phase, history design_hash); e2e |
| FT skipping a phase is refused | PASS | `a_skip_ahead_or_foreign_target_is_refused` |
| FT a blocked readiness line cannot leave architecture | PASS | `only_a_passing_readiness_line_leaves_architecture` |
| FT a FIX round is a rewind with a reason | PASS | `a_fix_round_is_a_rewind_with_a_reason`; e2e `a_fix_round_and_the_evidence_check_answer_through_the_router` |
| FT a rewind without a reason is refused | PASS | the same two tests (none, `""`, `"   "`), plus the abandon case in `closing_from_a_middle_phase…` |
| FT an uncalled tool is reported absent | PASS | `the_evidence_check_reports…`; e2e (observer ring holds `run_erc` error then ok, so confirmed is `[run_erc]` and absent is `[render_schematic_png]`; the transition happens) |
| FT an architecture gate approval requires a passing readiness line | PASS | `approving_architecture_against_a_blocked_readiness_writes_no_gate_file` (message names BLOCKED and its reason) |
| FT an approval without the user's own words is refused | PASS | `an_approval_without_the_users_words_is_refused_in_guided_mode_and_at_purchase` (`""`, `"   "`) |
| FT autonomous mode still stops at purchase | PASS | e2e `an_autonomous_job_approves_placement_itself_and_still_stops_at_purchase`: one job crosses both gates. The unit tests used two separate jobs. |
| FT an approval binds to the design hash at that moment | PASS | `autonomous_mode_lets_the_session_approve_placement` checks STATE.md. e2e `a_placement_approval_binds_to_the_design_and_a_later_change_blocks_routing` checks that the response, STATE.md and `gates/placement.md` (`- design_hash: \`<hash>\``) all equal `flow_status`'s design_hash. |
| FT a design changed after the package was produced cannot be approved | PASS | `a_board_changed_since_the_gate_was_entered_is_a_stale_target` |
| FT advancing past a gate whose approval hash is stale is refused | PASS | e2e: after the placement approval, a board byte changes and `→ routing` is `stale_target` on `gate:placement` with "gate \"placement\"". It re-opens after the bytes are restored. Also `leaving_a_gate_requires_a_current_visit_approval_with_matching_keys`. |
| FT a rewind and return asks again | PASS | `a_rewind_into_a_gate_records_the_keys_its_next_approval_needs`; `flow_gate_e2e::a_design_change_after_approval_forces_a_rewind_and_a_new_approval` |
| FT a decision without a reason is refused | PASS | `a_decision_needs_why_and_rollback…`; e2e (log unchanged via `read: [log]`) |
| FT a decision without a rollback is refused | PASS | the same two tests |
| FT evidence needs no reason or rollback | PASS | the same two tests (message only, `read_name: log`) |
| FT each handoff gets its own file | PASS | `each_handoff_gets_its_own_numbered_file`; e2e (01-review.md, 02-review.md, verbatim) |
| FT a deferred finding is recorded | PASS | `a_deferred_item_lands_in_its_list_with_its_phase`; e2e (all 3 kinds, each in its list) |
| FT hostile text survives a round trip | PASS | unit (without `ç`) plus e2e (with `ç`, via the tools) |
| FT concurrent calls do not lose entries | PASS | `two_concurrent_defers_both_land`; e2e (two concurrent router calls land) |
| HO the router points to the orchestration reference | PASS | `every_reference_is_reachable_from_its_parent_skill`; AR-guard `orchestration_reference_keeps_its_protocol_rules` (Orchestrator section: "A flow job: read `references/orchestration.md`" and a link to each reference) |
| HO a bounded edit stays on the direct lane | PASS | The same guard pins "The Decision Tree below; no job" (SKILL), the §1 row "Bounded edit … \| none \| The konnect skill's Decision Tree", and the Orchestrator before the Decision Tree. The SKILL diff removes 0 lines. |
| HO the single-agent lane is named | PASS | The guard pins the §1 row "\| none \| One complete brief to that agent; check its handoff (§6) \|" |
| HO a fourth FIX round is not sent automatically | PASS | The guard pins §5 "**Three rounds at most.** … `fix_rounds` …" and "… stop: do not dispatch a fourth round. Report `BLOCKED` to the user". Probe: removing it turns the test red. |
| HO an implementation fault goes back to its producer | PASS | The guard pins the §5 rows `requirement`→`requirements`, `architecture`→`architecture`, and `implementation` → "`routing` for a routing defect found at `prefab_review`" |
| HO a Codex session runs the architecture phase | PASS | The guard pins §12 "kicad-architecture for `requirements` and `architecture`" and "as the producer, calls `flow_advance` itself" |
| HO an invented value blocks the readiness line | PASS | The guard pins §4 "…A `Readiness: BLOCKED` line names the missing or invented value" |
| HO an unverifiable claim is not accepted as DONE | PASS | The guard pins §6 "Any cited call in `absent` or `not_ok` makes the handoff a `FIX` with `failing_layer: implementation`", "Never re-run the call yourself…", `get_recent_calls(limit: 0)` and `calls.jsonl` |
| HO a FIX verdict names its failing layer | PASS | The guard pins the handoff-template header lines and "**`failing_layer`** is required on `FIX`" |
| HO a brief names what the agent may and may not touch | PASS | The guard checks brief-template's 8 field rows (Files Scope, Tools Granted and Tools Blocked are separate rows) and "Files Scope never includes `STATE.md`" |
| AR every new agent preloads real bundled skills | PASS | `agents_preload_existing_skills` (all 11 agents); exact lists in `orchestrator_agents_keep_their_contract` |
| AR every new agent declares an explicit turn budget | PASS | AR-guard `orchestrator_agents_keep_their_contract` (`maxTurns: <n>` present and equal to D9) |
| AR an agent that records a phase loads the flow toolset | PASS | `agents_make_claimed_evidence_executable` (7 cases); QA `every_agent_that_names_a_flow_tool_loads_flow` (derived from the directory, 9 agents enrolled; the sourcing-agent probe goes red) |
| AR a new skill's reference is reachable from its SKILL.md | PASS | `every_reference_is_reachable_from_its_parent_skill` |
| AR the record rules an agent needs are preloaded | PASS | AR-guard `record_sections_are_stated_where_their_writer_loads_them`. The 4 kicad-architecture records' sections are in the SKILL.md body that both writers preload. manufacturing.md, schematic-evidence.md, placement/routing and ledger sections are in the writing agents' own files. Probe: dropping "## BOM integrity" turns it red. |
| AR a forgotten manifest entry fails the build's tests | PASS | `manifest_ships_every_asset`; the probe file is named in the failure |
| AR no bundled agent is left unrouted | PASS | `top_level_skill_routes_every_bundled_agent`; `ls agents/*.md` = 11 |
| AR schematic build states its flow-record step | PASS | AR-guard Step 9 markers and order; frontmatter sha256 identical to bbd5efb |
| AR the layout agent still stops after placement | PASS | AR-guard Phase 3 markers ("Phase 3 ends the run", `gate:placement` with `placement.md`, "Never route in the same run") |

3. **Battery at `62d2045`** (log excerpts, one `cargo` process at a time, no concurrent build at start):
```
fmt exit=0 · clippy exit=0 warnings=0 errors=0 · build exit=0
konnect: asset_references 18 passed · doc_tool_counts 6 · not_tools_allowlist 3 · protocol_stdio 18 · bin 58 · lib 23 → konnect exit=0
konnect-core: lib 1442 passed, 16 ignored · conformance 4 · flow_contract_e2e 11 · flow_gate_e2e 3 · integration 12 · photo_intake_gate_e2e 5 (+1 ignored) → core exit=0
router:: 19 passed (incl. registry_tool_counts_match_reality, fixed_records_…, flow_exposes_…) · tools::flow:: 56 passed · install:: 16 passed
```
4. **SC3 deliberate-removal probes.** Each probe was one `mutate.py` call (mutate, run, restore, sha256 compare). Every run printed `1 failed`, then `restored byte-identical: True`, and `git status --short` stayed empty.
   - M1: kicad-architecture/SKILL.md, "`located, not validated` keeps the line" → "an unvalidated datasheet…". Output: `kicad-architecture/SKILL.md readiness line lost: a catalogue-only stock or a Datasheet cell reading \`located, not validated\` keeps the line …`
   - M2: kicad-architecture-agent.md Step 6, re-added "Catalogue stock is discovery; it is re-checked before payment." Output: `Step 6 again says \`before payment\``
   - M3: orchestration.md §4, dropped "its `## Checks at the purchase gate` section". Output: `orchestration.md §4 lost: …`
   - M4: architecture-record-schema.md, deleted the bullet "- its Datasheet cell reads `located, not validated`;". Output: `"A confirmed row" lost: …`
   - The same mechanism turned `orchestration_reference_keeps_its_protocol_rules` red (removed the §5 fourth-round text) and `record_sections_are_stated_where_their_writer_loads_them` red (removed `## BOM integrity`).
5. **New QA tests (all additive).**
   - `crates/konnect-core/tests/flow_contract_e2e.rs`: 11 tests. It uses the same harness as `flow_gate_e2e`: load through `ToolRouter`, validate with the compiled schema, dispatch through `ToolDef`. `list_toolboxes`/`load_toolset` go through `router::meta_tools::handle_meta_tool`.
   - `crates/konnect/tests/asset_references.rs`: 5 guards appended, plus helpers `flat()` and `missing_markers()` beside the existing `section()`. The guards are `architecture_confirmation_rule_stays_guarded`, `orchestration_reference_keeps_its_protocol_rules`, `orchestrator_agents_keep_their_contract`, `every_agent_that_names_a_flow_tool_loads_flow` and `record_sections_are_stated_where_their_writer_loads_them`. No existing function was edited.

## For the next agent

1. The prose guards match whitespace-flattened phrases, scoped to a section: an H2/H3 heading up to the next heading. Re-wrapping text will not break them. Rewording a guarded sentence, or renaming a guarded heading, will: `## 9. Sourcing inside \`architecture\``, `**Step 6: Parts that can be bought**`, `### The readiness line`, `**A confirmed row.**`, `### Recording the review (flow job only)`, `### \`manufacturing.md\` — required sections`. When a guard fails, update its marker in the same commit and say why.
2. `every_agent_that_names_a_flow_tool_loads_flow` is derived from the agents directory and asserts at least 9 agents are enrolled. A new agent that names any `flow_*` tool must also load `flow`. `agents_make_claimed_evidence_executable` remains a hard-coded list, and sourcing and library are not in it.
3. The e2e records calls into `ctx.observer` directly to feed the evidence check. This matches how the handler records calls; it does not go through `mcp::handler`. The purchase-gate and close-around-gate paths are covered in both `guided` and `autonomous` mode.
4. Another agent ran `cargo test -p konnect-core --test flow_gate_e2e` in this same worktree during my run (pids 20184/4188). Each probe mutated a file for one test build only, then restored it byte-identical. A red run someone saw during that window is a probe, not a regression.

## Deferred findings

1. `crates/konnect-core/src/tools/flow.rs:3232`: `HOSTILE_OBJECTIVE` has no `ç`, although task 1.1's criterion names it. The behaviour is covered by my e2e. A one-character edit would make the in-crate test match the criterion. Owner: dev, test hygiene.
2. No guard checks README's "12 skills + 11 agents" row (README.md:345), so it will go stale on the next manifest change. This repeats 09-developer deferred finding 2. Fix: derive the counts from `SKILLS.len()`/`AGENTS.len()` in a konnect test. Owner: test-hygiene backlog.
3. `flow_status`'s `gate_validity` recomputes `valid` from the hashes and not the visit (04-developer deferred finding 2). An approval from an earlier visit, left there only by a hand edit, would show `valid: true` while `flow_advance` refuses to leave on it. No criterion covers this. Owner: dev backlog.
4. Pre-existing, not in this change: `konnect/SKILL.md` and five older agents cite `references/reliability-contract.md`, which does not exist (06-developer deferred finding 1). None of the six new agents cite it. Owner: docs backlog.
