---
change: konnect-orchestrator
task: tasks 2.1-2.3, 3.1-3.3 — companion skills, orchestration reference, templates, router
agent: developer
verdict: DONE
failing_layer: null
---

## Result

1. Six commits on `orc/konnect-orchestrator` (worktree `C:\Users\felip\.orc\worktrees\konnect-3b7e2022\konnect-orchestrator`, on top of `b978aa5`), not pushed: `9878c1e` 2.1 `kicad-architecture/SKILL.md` + `references/constraint-record-schema.md`; `0a3ed3f` 2.2 `references/architecture-record-schema.md`; `9a6ca2c` 2.3 `kicad-curator/SKILL.md` (no `references/`); `c0e51df` 3.1 `konnect/references/orchestration.md`; `b7403b6` 3.2 `konnect/references/brief-template.md` + `handoff-template.md`; `d0eb0de` 3.3 `konnect/SKILL.md` (+54/-0).
2. 3.3 inserts three blocks and changes no line: `## Orchestrator — \`/konnect <request>\`` right before `## Decision Tree` (lanes table + three-row Need → Read table), `| Orchestration | flow |` after the Manufacturing row, and six Agent Routing bullets after the `kicad-design-review-agent` bullet, before "Keep small, bounded edits". The file stays CRLF throughout.
3. Every `flow_*(…)` example in the new files is written as a bare signature naming every required parameter, e.g. `flow_status(project_dir, read)`, never `flow_status(read)`. Values go in prose ("`kind` is `decision`"), so `call_examples_name_real_parameters` checks all of them.
4. Decision the design left open: the curator's **playbook note** (a job with ≤1 FIX round in total, summed over `fix_rounds`) is one `flow_log` lesson with `role` `orchestrator`, `scope` `role`, a Lesson line starting `Playbook:`, and `Promote: no`. It waits in `records/lessons-candidates.md` for the user's review, which is the doc's "fila de revisão do cérebro". It never goes into a 60-line MEMORY.md.
5. The asset suite is fully green except `backticked_tool_names_in_prose_exist_in_the_registry`, which was green at `b978aa5` and is now red with 46 flags in 4 files (expected until 6.2; list in "For the next agent" 4). `konnect/SKILL.md` and `kicad-architecture/SKILL.md` add no flagged name.

## Evidence

1. Acceptance greps (worktree, after each commit). 2.1: SKILL names `constraint-record-schema.md` 1× and `architecture-record-schema.md` 1×; `Readiness: PASS` 4×, `Readiness: BLOCKED` 3×. 2.2: `design-calculations.md` 1, `jlcpcb-rules.md` 1, `datasheet-audit.md` 2, last-line rule = D5 (it mirrors `parse_readiness`: exact `PASS`, a `BLOCKED` with an empty reason is malformed). 2.3: the three destinations 2/3/2×; the file's only `flow_log(` names all four. 3.1: `grep -c "evidence_check\|get_recent_calls"` = 3, `Readiness: PASS` = 2, all 9 phase agents plus both photo agents named. 3.2: all eight fields; `Files Scope never includes \`STATE.md\`` 1; `failing_layer` 3, `is required on \`FIX\`` 1.
2. `cargo test -p konnect --test asset_references -- documented_toolsets_exist_in_the_registry call_examples_name_real_parameters tools_listed_beside_a_toolset_belong_to_it top_level_skill_routes_every_bundled_agent every_reference_is_reachable_from_its_parent_skill scripted_board_fallback_stays_guarded` → `6 passed; 0 failed`. `cargo test -p konnect --test doc_tool_counts` → `6 passed` after every commit. `library_ids_in_guidance_resolve_against_installed_kicad` → ok (KiCad installed).
3. `git diff --numstat bbd5efb..HEAD -- crates/konnect/assets/skills/konnect/SKILL.md` → `54 0`, and `grep -c '^-[^-]'` on that diff → `0`. `git diff --name-only b978aa5..HEAD` → the 8 FS asset files only, in 6 commits.
4. Mutation proofs (mutate → run → restore in one script, each `restored byte-identical: True`). (a) A signature with `job_id` dropped and `reviewer` added in `kicad-architecture/SKILL.md:85` fails `call_examples…`, which names both. (b) `run_erc` in orchestration.md's `load_toolset("flow")  # …` comment fails `tools_listed_beside…` ("lives in 'sch_export'"). (c) Unlinking `brief-template.md` from `konnect/SKILL.md` fails `every_reference_is_reachable…` naming it.
5. `cargo test -p konnect --no-fail-fast`: every target passes (plugin lib 23, main 57, doc_tool_counts 6, not_tools_allowlist 3, protocol_stdio 18, schema_parameter_usage 5, …). The one exception is asset_references, at `12 passed; 1 failed` (the phantom test).

## For the next agent

1. **Quote exactly — records and sections.** Records (D4 enum): `constraints.md`, `architecture.md`, `worst-case.md`, `pin-plan.md`, `schematic-evidence.md`, `ledger-schematic.md`, `placement.md`, `routing.md`, `ledger-prefab.md`, `manufacturing.md`. Required headings (kicad-architecture SKILL body): constraints → `## Scope`, `## Constraints`, `## Decisions`, `## Open questions`; architecture → `## Blocks`, `## Power tree and budget`, `## Interfaces`, `## Parts list`, `## Open questions`, then the readiness line last; worst-case → `## WC-<n> — <the value decided>` (IDs never reused); pin-plan → `## <reference> — <part>` + `| Pin | Function | Net | Constraint | Source |`. Parts-list columns (sourcing returns rows in exactly this shape) are in `architecture-record-schema.md` §1 "Parts list". Library column value for a part to make: `needs library`.
2. **Quote exactly — readiness and calls.** Readiness: `Readiness: PASS` / `Readiness: BLOCKED — <value and what would supply it>`. Call shapes used everywhere: `flow_status(project_dir, read)`, `flow_start(project_dir, objective, lane, phases, mode)`, `flow_advance(project_dir, job_id, to_phase, records, evidence_calls)`, a rewind `flow_advance(project_dir, job_id, to_phase, reason)`, leaving a gate `flow_advance(project_dir, job_id, to_phase)`, `flow_gate(project_dir, job_id, gate_name, decision, summary, user_words)`, `flow_log(project_dir, job_id, kind, message, why, rollback)` (decision), `flow_log(project_dir, job_id, kind, message, role, scope)` (lesson), `flow_log(project_dir, job_id, kind, message, role)` (handoff), `flow_defer(project_dir, job_id, kind, description, owner)`. Handoff header: `job_id`, `phase`, `role`, `verdict`, `failing_layer`. Sections: `## Result`, `## Evidence`, `## For the next agent`, `## Deferred findings`, `## Questions`.
3. **Section names the agents can cite.** orchestration.md §2 phase playbook, §4 gates, §5 FIX loop, §6 evidence cross-check, §7 resume, §8 one question round, §9 sourcing, §10 library, §11 learn, §12 Codex. The kicad-curator SKILL has "The triage rule", "The candidate entry format" (`Lesson:`/`Evidence:`/`Scope:`/`Role:`/`Promote:`), "At `learn` — distilling the job" (steps 1–6, step 6 = close with `flow_advance` to `closed`), and "Not a brain". kicad-architecture has "Requirements — the constraint record" and "The readiness line" (the architecture agent must cite "BLOCKED means return BLOCKED, not advance").
4. **Task 6.2 input**, from the red phantom run at `d0eb0de`. 21 distinct names: `approved_by`, `board_revision`, `deferred_findings`, `evidence_check`, `fab_only`, `failing_layer`, `fix_rounds`, `gate_approvals`, `last_transition`, `lock_files`, `new_board`, `next_step`, `not_ok`, `pending_approval`, `pending_approvals`, `photo_to_kicad`, `prefab_review`, `queue_item`, `review_only`, `schematic_review`, `state_error`. Only `not_ok` is outside D10 item 2's expected list (it is an `evidence_check` field, D6). The agent round will add more (e.g. `design_hash` if quoted). Re-run the test after round 2b; do not trust this list alone.
5. **Gotchas.** (a) A shorthand like `flow_status(read)` or `flow_gate(architecture, approve)` is a bare signature, so it fails `call_examples…`: use the full shape or keep values out of the parentheses (`to_phase: closed` with a colon is skipped). (b) At `c0e51df` and `b7403b6`, `every_reference_is_reachable_from_its_parent_skill` is red. The konnect references exist before 3.3 names them, as the task order dictates, and `d0eb0de` restores it. (c) Backticked `Lib:Symbol` tokens are checked against installed KiCad: `gate:architecture` is safe (there is no `gate` library), and the only IDs I quoted are `Device:C`. Footprint IDs were left unbackticked.

## Deferred findings

1. `references/reliability-contract.md` does not exist in the repo, yet it is cited by `konnect/SKILL.md:74` ("Standard Workflow") and by all five existing agents (`kicad-design-review-agent.md:23`, `kicad-pcb-layout-agent.md:27`, `kicad-schematic-build-agent.md:21`, `pcb-design-reconstruction-agent.md:28`, `pcb-photo-intake-agent.md:31`). `git ls-tree main` lists only `operating-notes.md` under `konnect/references/`. No guard checks a link against a missing file (`every_reference_is_reachable…` only checks existing file → SKILL). The round-2b agents should not copy that sentence until the file exists or the citation is removed.
