---
change: konnect-orchestrator
task: verify fix round 1 (tasks 8.1–8.9, new flow-toolset scenarios, full battery)
agent: qa
verdict: DONE
---

## Result

**DONE.** All 9 tasks pass, 25 of 25 acceptance clauses. The 4 new flow-toolset scenarios pass, and each of reviewer 11's findings is closed by a test that runs. The full battery is green at final HEAD `1c05a85`.

I added two `test(...)` commits (not pushed):
- `28ab0b8`: a new file, `crates/konnect-core/tests/flow_fix_round_one_e2e.rs`, with 4 router-level tests.
- `1c05a85`: 3 guards appended to `crates/konnect/tests/asset_references.rs` (+144 / -0).

No implementation file or earlier test was edited. `flow_contract_e2e.rs` has 0 diff lines against `62d2045`, and `asset_references.rs` has 0 removed lines.

On developer 14's two points:
1. **Manufacture READY is effectively unreachable inside the phase.** This is correct, and it is what DECISION F asks for ("'Only READY permits upload' happens after the gate"). The skill supports it:
   - `kicad-manufacture/SKILL.md:15` says a required check that cannot be established makes the verdict `INCOMPLETE`.
   - Steps 5 and 6 (viewer, order preview) are required acceptance steps.
   - The completion criterion at `:207-208` includes "visual/order previews agree".
   - Under the literal design wording (READY = only the agent's own checks), READY would absorb the three open checks, and task 8.7's "INCOMPLETE whose only open items are the three" exception could never happen.

   I scored it PASS. `design.md:585-586` still paraphrases READY more narrowly (Deferred 3).
2. **The replaced table row.** A script confirms that the old `manufacturing` row, minus its closing ` |`, is a strict prefix of the new row. It is the only removed line in `orchestration.md`, so the row change is a pure insertion. PASS.

### Per criterion (evidence at `1c05a85` unless noted)

- **8.1** Unit test gains both cases, each `Err` naming the gate and phase — **PASS**. `tools::flow::foundation_tests::the_validator_names_the_entry_it_rejects -- --exact`: 1 passed.
- **8.1** `flow_gate_e2e::a_gate_without_its_phase_is_refused_through_flow_start` exists: `invalid_argument`, field `phases`, names `manufacturing`, no `.konnect/flow` — **PASS** (flow_gate_e2e 4 passed).
- **8.1** `flow::` and `--test flow_gate_e2e` pass — **PASS** (61 and 4). Red at `62d2045`: my e2e opened `[routing, prefab_review, gate:purchase]` (`"phase":"routing"`, no error).
- **8.2** `rewind_tests::a_rewind_cannot_target_a_gate`: `invalid_argument` naming `"gate:placement"` and `"placement"`, `STATE.md` bytes unchanged — **PASS** (1 passed). Red at `62d2045`: my e2e's rewind to `gate:architecture` was accepted (`cleared_approvals:["architecture"]`, `phase:"gate:architecture"`).
- **8.2** `flow::` passes — **PASS** (61).
- **8.3** `gate_tests::a_gate_file_write_failure_after_state_commits_is_a_warning` — **PASS**: success, `warning` names `records\gates`, status `current`.
- **8.3** `advance_tests::a_log_append_failure_after_state_commits_is_a_warning` — **PASS**: success, `warning`, phase moved.
- **8.3** `flow::` and `flow_gate_e2e` pass — **PASS**. Red at `62d2045`: my router test failed with `Could not create …\flow\records\gates (… os error 183)`.
- **8.4** `status_tests::a_passed_gates_approval_reports_status_passed_without_a_valid_field` — **PASS**: `passed`, no `valid`, hash unchanged; at `gate:placement` it reports `current` with a recomputed `valid`.
- **8.4** `gate_validity_is_recomputed_against_the_current_hashes` — **PASS**. It now stands at `gate:architecture`; that rewrite is justified by DECISION D.
- **8.4** `flow::` passes — **PASS**.
- **8.5** `grep -c "a gate's phase must be present too"` = 1, inside §1 — **PASS**.
- **8.5** `status: passed` = 1, inside §7 — **PASS**.
- **8.5** `never repeat the call` = 1 — **PASS**.
- **8.5** `orchestration_reference_keeps_its_protocol_rules` — **PASS**.
- **8.6** Step 6 has 2 matching lines for `register_symbol_library\|register_footprint_library` — **PASS**.
- **8.6** The sentence names `scope: "project"` and the scratch project's path — **PASS**.
- **8.6** `asset_references` passes in full — **PASS** (21).
- **8.7** `git diff -- …/kicad-manufacture/SKILL.md` is empty; `bbd5efb..HEAD` for that file is also empty — **PASS**.
- **8.7** `passed with a warning` = 1 — **PASS**.
- **8.7** "which may exit to \`gate:purchase\`" = 1 — **PASS**.
- **8.7** The three named guards pass (3 passed, 18 filtered) — **PASS**.
- **8.8** `asset_references` passes in full, with my five round-1 guards unmodified — **PASS**.
- **8.8** `--lib flow::` 61, `flow_gate_e2e` 4 and `flow_contract_e2e` 11, with 0 edits to `flow_contract_e2e` — **PASS**.
- **8.8** `fmt --check` and `clippy --all-targets` both exit 0 — **PASS**.
- **8.9** `journal_tests::a_defer_log_failure_after_state_commits_is_a_warning`: success with `warning`, item in `STATE.md` — **PASS** (1 passed). Red at `421aa56`: my router test failed with `defer with the log blocked failed: Could not create …\flow\log (os error 183)`, and my P2 test failed at `flow_defer`.
- **8.9** `flow::` and both e2e binaries pass — **PASS**.

### New flow-toolset scenarios

- **A gate already left reports passed, not a recomputed validity** — **PASS**. The unit test above covers it, and so does `side_files_follow_the_state_commit_through_the_router`: after a schematic save the entry is `passed` with no `valid`, and `design_hash_at_approval` equals the approval's. `package_hash_at_approval` is a non-optional field (`flow.rs:396`), and the entry is the whole serialized approval.
- **A gate without its producing phase is rejected** — **PASS**. `reviewer_11_lane_shapes_are_refused_after_a_closed_job_left_its_records` runs all three gates through the router.
- **A rewind cannot target a gate** — **PASS**. `a_rewind_into_a_gate_is_refused_and_the_producer_route_re_asks` covers it.
- **A side-file failure after `STATE.md` commits is a warning, not a lost decision** — **PASS**. The router test covers all three tools. The warning text contains "Do not repeat the call".

### Reviewer 11 finding → the test that closes it

| Finding (tasks.md numbering) | Closing tests |
|---|---|
| SERIOUS, shapes A and B | Unit: `the_validator_names_the_entry_it_rejects` (2 cases). E2e: `flow_gate_e2e::a_gate_without_its_phase_is_refused_through_flow_start` (shape A). **QA** `reviewer_11_lane_shapes_are_refused_after_a_closed_job_left_its_records` reproduces P1 through the router: job A (fab_only) closes with REV A `manufacturing.md`, and a stale `Readiness: PASS` `architecture.md` is planted. Shape A, shape B and `[schematic, schematic_review, gate:placement, routing]` are each refused, and the `.konnect/flow` fingerprint is unchanged. The corrected lane must re-supply `manufacturing.md` in this job, and rewriting it after the approval blocks `closed`. |
| Minor 1: side files after the commit | Unit: gate, advance and defer warning tests. **QA** `side_files_follow_the_state_commit_through_the_router` (gate, advance, defer through the schemas; `warning` is null once the log is writable). **QA** `a_failed_state_commit_leaves_no_side_file_claiming_the_change` (`#[cfg(windows)]`) holds `STATE.md` with `share_mode(FILE_SHARE_READ)`, which is reviewer P2's primary direction and the case developer 13 called untestable. Reject, defer and advance each return an error, and the fingerprint is unchanged: no gate file or log line says reject. Once the handle is released, the committed approval still opens the gate. |
| Minor 2: passed/current | Unit: `a_passed_gates_approval_…`, `gate_validity_is_recomputed_…`. **QA** router: `side_files_…` (passed) and `a_rewind_into_a_gate_…` (current, `valid: true` after re-approval). |
| Minor 3: rewind into a gate | Unit: `a_rewind_cannot_target_a_gate`. **QA** router: refused with and without `reason`, nothing written. The positive half also passes: rewinding to `architecture` clears the approval, the re-entered gate has no approval, leaving is refused, and re-approving then leaving succeeds. |
| Minor 4: library text | **QA** `library_agent_registers_its_library_in_the_scratch_project` checks the markers, plus that registration comes before "Then place the symbol". `call_examples_name_real_parameters` checks the argument names. |
| Minor 5: manufacture text | **QA** `manufacture_agent_verdicts_match_the_skill`. It has a negative marker for "and only the purchase-gate checks remain", checks the Verdict, Ending the run and Record the phase sections, and pins the skill's own `INCOMPLETE` / "Only `READY` permits upload" lines. |
| Minor 6: flow-loading guard | My round-1 `every_agent_that_names_a_flow_tool_loads_flow` and `architecture_confirmation_rule_stays_guarded` still pass unmodified. |

## Evidence

- **Baseline battery at `94d2944`, before my tests.** fmt 0; clippy 0; `konnect-core`: lib 1447 passed / 16 ignored, flow_contract_e2e 11, flow_gate_e2e 4, all other binaries ok; `konnect`: asset_references 18, main 58, protocol_stdio 18, doc_tool_counts 6, not_tools_allowlist 3, schema_parameter_usage 5, all ok.
- **Final battery at `1c05a85`, clean tree.**
  - `cargo fmt --check`: exit 0.
  - `cargo clippy --all-targets`: exit 0.
  - `cargo test -p konnect-core`: exit 0. lib 1447 passed / 16 ignored; flow_contract_e2e 11; flow_fix_round_one_e2e 4; flow_gate_e2e 4; conformance 4; integration 12; photo_intake_gate_e2e 5 (+1 ignored live); prepare_board_photo_contract 6; reuse 1; two_checkpoint 3.
  - `cargo test -p konnect`: exit 0. plugin 23; main 58; asset_references 21; doc_tool_counts 6; not_tools_allowlist 3; protocol_http 2; protocol_stdio 18; release_workflow 2; schema_migrations 3; schema_parameter_usage 5; transaction_cli 4.
  - Every filtered run above printed a count of at least 1.
- **Red proof of the new e2e tests, without touching this worktree.** I ran them in a detached scratch worktree using the same target directory, then removed it.
  - At `62d2045`, all 4 failed, each at its intended assertion.
  - At `421aa56`, the lane and rewind tests passed. `side_files_…` failed at the defer, and the P2 test failed at `flow_defer`. So the P2 test separates 8.3 from 8.9.
- **Red proof of the new asset guards.** A mutate → run → restore script (`scratchpad/mutate.py`) found 6 of 6 mutations RED, each with `restored identical: True` (sha256):
  - M1: §1 converse sentence dropped.
  - M2: §7 "never repeat" changed to "repeat if unsure".
  - M3: §2 exception removed.
  - M4: "Before placing" changed to "After placing".
  - M5: placement moved ahead of registration (the order assertion).
  - M6: the old READY loosening re-inserted (negative marker plus lost marker).
- **Concurrency.** During the round, another agent ran `cargo build -p konnect` into `%TEMP%\k16t`. Before mutating assets I confirmed that no cargo or rustc process was running.

## For the next agent

- The change is ready for the orchestrator's next step. No FIX items.
- `flow_fix_round_one_e2e.rs`'s P2 test is `#[cfg(windows)]` because it relies on Windows share modes. On a non-Windows CI the primary direction is covered only by construction.

## Deferred findings

1. **The published descriptions of `flow_gate`, `flow_advance` and `flow_defer` never mention `warning`.** `flow.rs:3160-3340` has 0 matches (developer 14 #2, confirmed). A client without the konnect skill cannot learn the no-retry rule. Fix: one clause per description.
2. **Post-commit side writes run outside the `STATE.md` lock** (developer 13 #2 / 14 #1, still open). Concurrent gate files or log lines can land out of commit order; `STATE.md` stays the truth.
3. **`design.md:585-586` defines READY as only "every check the agent's tools can run passed with no warning"**, which is narrower than the agent text and the skill (`SKILL.md:15`). Doc amendment: add "and no purchase-gate check is still open (SKILL.md:15; DECISION F)".
4. **Library Step 6 is guarded as text only.** Nobody has checked live that a scratch-project registration makes `place_component` or `add_schematic_component` resolve the part, or whether the stored URI is absolute or `${KIPRJMOD}` (developer 14 point 4). This needs one live KiCad run.
5. **`flow_advance`'s description says "a refusal writes nothing".** Under D2, a forward advance whose `STATE.md` rename fails at the I/O level still leaves its phase records on disk. That is harmless by D4's re-supply rule, but the sentence overstates it. This predates the round; low priority.
