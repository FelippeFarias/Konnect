---
change: konnect-orchestrator
task: final verify, round 3 of 3 (tasks 9.1–9.6, a spot re-check of §1–8, the full battery)
agent: qa
verdict: DONE
---

## Result

**DONE. All 9.1–9.6 criteria PASS at `9e848d3`, and the full battery is green there.** `9e848d3` is my `test(konnect)` commit on top of `e2228f9`, on `orc/konnect-orchestrator`. Nothing was pushed, and no implementation or openspec file was edited.

Before this round, only 9.3/9.4 had guards. The texts from 9.1, 9.2 and 9.6 had no executable check (developer 19 flagged 9.6). I added one for each, all section-scoped and whitespace-flattened, and showed each one RED on a deliberate reversion.

| Task | Criterion | Verdict | Evidence |
|---|---|---|---|
| 9.1 | `grep -c "only for the gate the job stands at now" flow.rs` ≥ 2 | PASS | 2 (`:664` STATE.md render, `:3107` flow_status) |
| 9.1 | `grep -c "never repeat the call"` ≥ 3; `"a validation refusal writes nothing"` = 1 | PASS | 3 (`:3180`, `:3236`, `:3331`); 1 (`:3178`) |
| 9.1 | `--lib flow::` and the three flow e2e suites pass, with zero behaviour change | PASS | lib 1447 passed; flow_gate_e2e 4, flow_contract_e2e 11, flow_fix_round_one_e2e 4. The diff `1c05a85..e2228f9 -- flow.rs` changes only string literals |
| 9.1 | (new) the published texts are guarded | PASS | `flow_fix_round_two_texts.rs`: 2 passed. RED when the texts are reverted (2 failed) |
| 9.2 | the READY-owner sentence appears once, inside `## 4. Gates` | PASS | 1, at `:143`; §4 is `:107`, §5 is `:158` |
| 9.2 | `"reports every approval's validity"` = 0; `"only for the gate the job stands at now"` = 1 | PASS | 0; 1 |
| 9.2 | the two round-1 orchestration guards pass | PASS | asset_references 24/24 |
| 9.2 | (new) guarded, including the bullet's position (after the purchase checks, before the rejection) | PASS | `orchestration_reference_gives_ready_an_owner_after_the_purchase_gate`. RED when the bullet is removed and RED when the old sentence is restored |
| 9.3 | `"every artifact check your tools can run passed"` = 1, inside Verdict | PASS | 1, at `:177`; Verdict spans `:176–:192` |
| 9.3 | `"every DRC warning and every preflight issue is adjudicated"` ≥ 1; `"and only the purchase-gate checks remain"` = 0 | PASS | 1; 0 |
| 9.3 | `"the orchestrating session declares that"` = 1, inside Ending the run | PASS | 1, at `:213`; the section spans `:199–:225` |
| 9.4 | both manufacture guards pass | PASS | 24/24 |
| 9.4 | splicing the pre-9.3 Verdict bullet back makes both guards fail | PASS | bullet taken from `1c05a85` and spliced in: 2 failed, 9 distinct messages; file restored byte-identical |
| 9.4 | `grep -c "left an artifact missing, or passed with a warning" asset_references.rs` ≥ 2 | PASS | 2 |
| 9.5 | asset_references in full, flow suites, `fmt --check`, `clippy --all-targets` | PASS | battery below |
| 9.6 | tool-directory: `recomputed \`valid\`` = 0; the current-gate phrase = 1 | PASS | 0; 1 (`:505`) |
| 9.6 | the frontmatter no longer says "ending in READY"; `model`/`skills`/`tools`/`maxTurns` unchanged | PASS | 0 hits in the first 12 lines. `git diff -U0 1c05a85..e2228f9` shows no `+`/`-` line for those keys (grep exit 1). `8965f00..e2228f9` touches only the 2 intended files |
| 9.6 | asset_references and doc_tool_counts pass | PASS | 24/24; 6/6 |
| 9.6 | (new) all three texts guarded | PASS | `decision_m_texts_match_the_fixed_behaviour`. RED on each of (a), (b) and (c) separately |

**How each reviewer-16 finding is closed:**

| Finding | Closing check |
|---|---|
| SERIOUS 1: warnings-only board cannot leave `manufacturing` | `manufacture_verdict_permits_exit_with_adjudicated_drc_warnings` checks (a): READY adjudicates DRC warnings and preflight issues; NOT READY blocks only on an unwaived DRC error; the INCOMPLETE clause names neither DRC nor preflight. It also checks (b): the artifact clause stays, with no `adjudicat`/`waive`/`accepted with` escape. `manufacture_agent_verdicts_match_the_skill` adds a stale-phrase check on the old wide wording. Both go RED on the pre-9.3 bullet. This is a prose-level stand-in: no live agent run on `ecc83-pp` (reviewer 16 did not run one either). |
| minor 2: `READY` has no owner after the purchase gate | new `orchestration_reference_gives_ready_an_owner_after_the_purchase_gate`, plus the new Ending-the-run marker `"the orchestrating session declares that"` in `manufacture_agent_verdicts_match_the_skill`, plus the 9.6(b) frontmatter guard |
| minor 3: three texts say `valid` is recomputed for every approval | flow.rs description and STATE.md render: `flow_fix_round_two_texts.rs` (through `ToolRouter::load("flow")` → `to_mcp_description()`, and a schema-validated `flow_start` that renders STATE.md). orchestration §4: the new §4 guard. tool-directory row: the 9.6(a) guard |
| minor 4: D11's cost of a false stale | closed by a spec edit, design.md `:811–:815`: "a re-run of the producing agent plus a re-approval". Prose in openspec that nothing reads at runtime; closed by the artifact changing |
| minor 5: post-commit side writes outside the lock | accepted by DECISION L (log `:56`). design.md records it at `:309–:323`: "Fix round 2 (reviewer 16 minor 5, DECISION L — accepted, not fixed)". No task, no check needed |

## Evidence

1. **Red proofs.** Each ran in a scratch worktree (`git worktree add --detach … 9e848d3`, target dir reused, then removed and pruned). Each mutated file was restored and its sha256 compared with the original:
   - 9.6(a) row → "gate approvals with a recomputed `valid`": 1 failed, "flow_status row lost … / again says `recomputed `valid``", restored byte-identical.
   - 9.6(b) → "ending in READY, NOT READY or INCOMPLETE.": 1 failed, 2 messages, byte-identical.
   - 9.6(c) → "(a warning,": 1 failed, 2 messages, byte-identical.
   - 9.2 bullet removed: 1 failed, 4 lost markers plus "positions Some(2135), None, Some(2258)", byte-identical.
   - 9.2 old sentence restored: 1 failed, 2 messages, byte-identical.
   - 9.1 flow.rs texts reverted: 2 failed (description and STATE.md body), byte-identical.
   - 9.4 pre-9.3 bullet: 2 failed, byte-identical.
2. **Battery at `9e848d3`** (clean tree, PROTOC and CMake set):
   - `cargo fmt --check`: rc 0.
   - `cargo clippy --all-targets`: rc 0, 0 warnings.
   - `cargo test -p konnect-core`: rc 0. lib 1447 passed / 16 ignored; conformance 4; flow_contract_e2e 11; flow_fix_round_one_e2e 4; flow_fix_round_two_texts 2; flow_gate_e2e 4; integration 12; photo_intake_gate_e2e 5 (+1 ignored); prepare_board_photo_contract 6; reuse 1; two_checkpoint 3.
   - `cargo test -p konnect`: rc 0. lib 23; main 58; asset_references 24; doc_tool_counts 6; not_tools_allowlist 3; protocol_http 2; protocol_stdio 18; release_workflow 2; schema_migrations 3; schema_parameter_usage 5; transaction_cli 4. Live/e2e KiCad tests ignored, as before.
3. **Spot check of §1–8.** All 49 task boxes are `[x]` and none is open. The suites that hold §1–8 stayed green at the final HEAD: the round-1 flow e2e (11 + 4), flow_gate_e2e, the 22 earlier asset guards, doc_tool_counts and not_tools_allowlist (which parses NOT_TOOLS from asset_references.rs, still 3/3 after my appends). Fix round 2 changed only flow.rs strings, so no §1/§8 behaviour moved.

## For the next agent

1. Files added: `crates/konnect-core/tests/flow_fix_round_two_texts.rs` (2 tests), and 2 tests appended to `crates/konnect/tests/asset_references.rs` (`orchestration_reference_gives_ready_an_owner_after_the_purchase_gate`, `decision_m_texts_match_the_fixed_behaviour`). Commit `9e848d3`. Not pushed.
2. The change is ready for archive from QA's side. The deferred item below is text-only and outside every task's scope.

## Deferred findings

1. `tool-directory.md`'s `flow_advance` row (`:507`) still says "A refusal writes nothing". Neither it nor the `flow_gate` (`:508`) or `flow_defer` (`:510`) rows name `warning` / "never repeat the call", which DECISION K put into flow.rs's published descriptions. DECISION M scoped 9.6 to the `flow_status` row only, so this is not a failed criterion. Fix: one clause per row, mirroring flow.rs `:3178–3181`, `:3235–3236` and `:3329–3331`, plus a marker in `decision_m_texts_match_the_fixed_behaviour`.
2. The SERIOUS-1 closure rests on a prose guard. No run of `kicad-manufacture-agent` against a warnings-only board (such as `ecc83-pp`) has shown it actually exits `manufacturing`.
3. Still open from round 1 (`15-qa.md`): library Step 6 has not been run live.
